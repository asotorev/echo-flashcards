{
  "decks": [
    {
      "name": "LeetCode #25 - Reverse Nodes in k-Group",
      "description": "Reverse nodes of a linked list k at a time",
      "cards": [
        {
          "id": "leetcode_25_reverse_nodes_in_k_group",
          "question": "Reverse Nodes in k-Group: Given the head of a linked list, reverse the nodes of the list k at a time, and return the modified list. k is a positive integer and is less than or equal to the length of the linked list. If the number of nodes is not a multiple of k then left-out nodes, in the end, should remain as is. You may not alter the values in the list's nodes, only nodes themselves may be changed.",
          "solutions": [
            {
              "name": "Recursion",
              "type": "optimal",
              "explanation": "Recursively process groups of k nodes. First count k nodes to verify full group exists. If full group found, recursively process remaining list, then reverse current k nodes by iteratively moving head nodes to point to recursive result. If less than k nodes remain, return unchanged. Clean recursive solution with O(n/k) stack space.",
              "languageImplementations": [
                {
                  "language": "python",
                  "semanticLines": [
                    "Define function reverseKGroup that accepts head as optional ListNode and k as integer and returns optional ListNode",
                    "Initialize cur to head to traverse and count nodes",
                    "Initialize group to zero as counter for nodes in current group",
                    "Start while loop that continues while cur exists and group is less than k",
                    "Move cur forward by setting cur to cur dot next",
                    "Increment group by one",
                    "Check if group equals k to verify full group exists",
                    "If full group, recursively call reverseKGroup with cur and k, store result in cur",
                    "If full group, start while loop that continues while group is greater than zero",
                    "If full group, get tmp as head dot next to save next node",
                    "If full group, set head dot next to cur to reverse pointer",
                    "If full group, set cur to head to move reversed node to front",
                    "If full group, set head to tmp to advance to next node",
                    "If full group, decrement group by one",
                    "If full group, set head to cur as new head after reversal",
                    "Return head as result"
                  ],
                  "codeLines": [
                    "def reverseKGroup(head: Optional[ListNode], k: int) -> Optional[ListNode]:",
                    "    cur = head",
                    "    group = 0",
                    "    while cur and group < k:",
                    "        cur = cur.next",
                    "        group += 1",
                    "    if group == k:",
                    "        cur = reverseKGroup(cur, k)",
                    "        while group > 0:",
                    "            tmp = head.next",
                    "            head.next = cur",
                    "            cur = head",
                    "            head = tmp",
                    "            group -= 1",
                    "        head = cur",
                    "    return head"
                  ]
                },
                {
                  "language": "javascript",
                  "semanticLines": [
                    "Define function reverseKGroup that accepts head and k as parameters with opening brace",
                    "Initialize cur to head to traverse and count nodes",
                    "Initialize group to zero as counter for nodes in current group",
                    "Start while loop that continues while cur exists and group is less than k with opening brace",
                    "Move cur forward by setting cur to cur dot next",
                    "Increment group by one",
                    "Close the while loop with closing brace",
                    "Check if group equals k to verify full group exists with opening brace",
                    "If full group, recursively call reverseKGroup with cur and k, store result in cur",
                    "If full group, start while loop that continues while group is greater than zero with opening brace",
                    "If full group, get tmp as head dot next to save next node",
                    "If full group, set head dot next to cur to reverse pointer",
                    "If full group, set cur to head to move reversed node to front",
                    "If full group, set head to tmp to advance to next node",
                    "If full group, decrement group by one",
                    "Close the while loop with closing brace",
                    "If full group, set head to cur as new head after reversal",
                    "Close the if block with closing brace",
                    "Return head as result",
                    "Close function with closing brace"
                  ],
                  "codeLines": [
                    "function reverseKGroup(head, k) {",
                    "    let cur = head;",
                    "    let group = 0;",
                    "    while (cur && group < k) {",
                    "        cur = cur.next;",
                    "        group++;",
                    "    }",
                    "    if (group === k) {",
                    "        cur = reverseKGroup(cur, k);",
                    "        while (group > 0) {",
                    "            const tmp = head.next;",
                    "            head.next = cur;",
                    "            cur = head;",
                    "            head = tmp;",
                    "            group--;",
                    "        }",
                    "        head = cur;",
                    "    }",
                    "    return head;",
                    "}"
                  ]
                },
                {
                  "language": "go",
                  "semanticLines": [
                    "Define function reverseKGroup that accepts head as pointer to ListNode and k as int and returns pointer to ListNode with opening brace",
                    "Initialize cur to head to traverse and count nodes",
                    "Initialize group to zero as counter for nodes in current group",
                    "Start for loop that continues while cur is not nil and group is less than k with opening brace",
                    "Move cur forward by setting cur to cur dot Next",
                    "Increment group by one",
                    "Close the for loop with closing brace",
                    "Check if group equals k to verify full group exists with opening brace",
                    "If full group, recursively call reverseKGroup with cur and k, store result in cur",
                    "If full group, start for loop that continues while group is greater than zero with opening brace",
                    "If full group, get tmp as head dot Next to save next node",
                    "If full group, set head dot Next to cur to reverse pointer",
                    "If full group, set cur to head to move reversed node to front",
                    "If full group, set head to tmp to advance to next node",
                    "If full group, decrement group by one",
                    "Close the for loop with closing brace",
                    "If full group, set head to cur as new head after reversal",
                    "Close the if block with closing brace",
                    "Return head as result",
                    "Close function with closing brace"
                  ],
                  "codeLines": [
                    "func reverseKGroup(head *ListNode, k int) *ListNode {",
                    "    cur := head",
                    "    group := 0",
                    "    for cur != nil && group < k {",
                    "        cur = cur.Next",
                    "        group++",
                    "    }",
                    "    if group == k {",
                    "        cur = reverseKGroup(cur, k)",
                    "        for group > 0 {",
                    "            tmp := head.Next",
                    "            head.Next = cur",
                    "            cur = head",
                    "            head = tmp",
                    "            group--",
                    "        }",
                    "        head = cur",
                    "    }",
                    "    return head",
                    "}"
                  ]
                }
              ],
              "timeComplexity": "O(n)",
              "timeDescription": "Linear time as each node is visited once during traversal and once during reversal",
              "spaceComplexity": "O(n/k)",
              "spaceDescription": "Recursion call stack depth proportional to number of groups which is n divided by k",
              "exampleWalkthrough": "Given head=[1,2,3,4,5], k=2: Check nodes 1,2 (group=2). Recurse on [3,4,5]. Returns [3,4,5]. Reverse 1,2: 2→1→[3,4,5]. Result: [2,1,4,3,5]. Node 5 unchanged as incomplete group.",
              "displayOrder": 1
            },
            {
              "name": "Iteration",
              "type": "optimal",
              "explanation": "Use dummy node and track group boundaries with pointers. For each group of k nodes, use helper function to find kth node. If found, reverse the group in place by iteratively redirecting pointers. Update group boundaries and continue. If less than k nodes remain, leave unchanged. Optimal O(1) space iterative solution without recursion overhead.",
              "languageImplementations": [
                {
                  "language": "python",
                  "semanticLines": [
                    "Define function reverseKGroup that accepts head as optional ListNode and k as integer and returns optional ListNode",
                    "Create dummy as ListNode with value zero and next set to head",
                    "Initialize groupPrev to dummy to track previous group end",
                    "Start infinite while loop with True",
                    "Call getKth helper with groupPrev and k to find kth node, store in kth",
                    "Check if kth does not exist using not operator",
                    "If no kth node, break from loop as no more full groups",
                    "Get groupNext as kth dot next to save start of next group",
                    "Initialize prev to kth dot next and curr to groupPrev dot next for reversal",
                    "Start while loop that continues while curr is not equal to groupNext",
                    "Get tmp as curr dot next to save next node",
                    "Set curr dot next to prev to reverse pointer",
                    "Set prev to curr to advance prev",
                    "Set curr to tmp to advance curr",
                    "Get tmp as groupPrev dot next to save original group start",
                    "Set groupPrev dot next to kth to connect to new group head",
                    "Set groupPrev to tmp to update to end of reversed group",
                    "Return dummy dot next as result list head",
                    "Define helper function getKth that accepts curr and k as parameters",
                    "Start while loop that continues while curr exists and k is greater than zero",
                    "Move curr forward by setting curr to curr dot next",
                    "Decrement k by one",
                    "Return curr as kth node or None"
                  ],
                  "codeLines": [
                    "def reverseKGroup(head: Optional[ListNode], k: int) -> Optional[ListNode]:",
                    "    dummy = ListNode(0, head)",
                    "    groupPrev = dummy",
                    "    while True:",
                    "        kth = getKth(groupPrev, k)",
                    "        if not kth:",
                    "            break",
                    "        groupNext = kth.next",
                    "        prev, curr = kth.next, groupPrev.next",
                    "        while curr != groupNext:",
                    "            tmp = curr.next",
                    "            curr.next = prev",
                    "            prev = curr",
                    "            curr = tmp",
                    "        tmp = groupPrev.next",
                    "        groupPrev.next = kth",
                    "        groupPrev = tmp",
                    "    return dummy.next",
                    "def getKth(curr, k):",
                    "    while curr and k > 0:",
                    "        curr = curr.next",
                    "        k -= 1",
                    "    return curr"
                  ]
                },
                {
                  "language": "javascript",
                  "semanticLines": [
                    "Define function reverseKGroup that accepts head and k as parameters with opening brace",
                    "Create dummy as new ListNode with value zero and next set to head",
                    "Initialize groupPrev to dummy to track previous group end",
                    "Start infinite while loop with true with opening brace",
                    "Call getKth helper with groupPrev and k to find kth node, store in kth",
                    "Check if kth does not exist using negation operator with opening brace",
                    "If no kth node, break from loop as no more full groups",
                    "Close the if block with closing brace",
                    "Get groupNext as kth dot next to save start of next group",
                    "Initialize prev to kth dot next and curr to groupPrev dot next for reversal",
                    "Start while loop that continues while curr is not equal to groupNext with opening brace",
                    "Get tmp as curr dot next to save next node",
                    "Set curr dot next to prev to reverse pointer",
                    "Set prev to curr to advance prev",
                    "Set curr to tmp to advance curr",
                    "Close the while loop with closing brace",
                    "Get tmp as groupPrev dot next to save original group start",
                    "Set groupPrev dot next to kth to connect to new group head",
                    "Set groupPrev to tmp to update to end of reversed group",
                    "Close the while loop with closing brace",
                    "Return dummy dot next as result list head",
                    "Close function with closing brace",
                    "Define helper function getKth that accepts curr and k as parameters with opening brace",
                    "Start while loop that continues while curr exists and k is greater than zero with opening brace",
                    "Move curr forward by setting curr to curr dot next",
                    "Decrement k by one",
                    "Close the while loop with closing brace",
                    "Return curr as kth node or null",
                    "Close function with closing brace"
                  ],
                  "codeLines": [
                    "function reverseKGroup(head, k) {",
                    "    const dummy = new ListNode(0, head);",
                    "    let groupPrev = dummy;",
                    "    while (true) {",
                    "        const kth = getKth(groupPrev, k);",
                    "        if (!kth) {",
                    "            break;",
                    "        }",
                    "        const groupNext = kth.next;",
                    "        let prev = kth.next, curr = groupPrev.next;",
                    "        while (curr !== groupNext) {",
                    "            const tmp = curr.next;",
                    "            curr.next = prev;",
                    "            prev = curr;",
                    "            curr = tmp;",
                    "        }",
                    "        const tmp = groupPrev.next;",
                    "        groupPrev.next = kth;",
                    "        groupPrev = tmp;",
                    "    }",
                    "    return dummy.next;",
                    "}",
                    "function getKth(curr, k) {",
                    "    while (curr && k > 0) {",
                    "        curr = curr.next;",
                    "        k--;",
                    "    }",
                    "    return curr;",
                    "}"
                  ]
                },
                {
                  "language": "go",
                  "semanticLines": [
                    "Define function reverseKGroup that accepts head as pointer to ListNode and k as int and returns pointer to ListNode with opening brace",
                    "Create dummy as address of ListNode with Val zero and Next set to head",
                    "Initialize groupPrev to dummy to track previous group end",
                    "Start infinite for loop with opening brace",
                    "Call getKth helper with groupPrev and k to find kth node, store in kth",
                    "Check if kth equals nil with opening brace",
                    "If no kth node, break from loop as no more full groups",
                    "Close the if block with closing brace",
                    "Get groupNext as kth dot Next to save start of next group",
                    "Initialize prev to kth dot Next and curr to groupPrev dot Next for reversal",
                    "Start for loop that continues while curr is not equal to groupNext with opening brace",
                    "Get tmp as curr dot Next to save next node",
                    "Set curr dot Next to prev to reverse pointer",
                    "Set prev to curr to advance prev",
                    "Set curr to tmp to advance curr",
                    "Close the for loop with closing brace",
                    "Get tmp as groupPrev dot Next to save original group start",
                    "Set groupPrev dot Next to kth to connect to new group head",
                    "Set groupPrev to tmp to update to end of reversed group",
                    "Close the for loop with closing brace",
                    "Return dummy dot Next as result list head",
                    "Close function with closing brace",
                    "Define helper function getKth that accepts curr as pointer to ListNode and k as int and returns pointer to ListNode with opening brace",
                    "Start for loop that continues while curr is not nil and k is greater than zero with opening brace",
                    "Move curr forward by setting curr to curr dot Next",
                    "Decrement k by one",
                    "Close the for loop with closing brace",
                    "Return curr as kth node or nil",
                    "Close function with closing brace"
                  ],
                  "codeLines": [
                    "func reverseKGroup(head *ListNode, k int) *ListNode {",
                    "    dummy := &ListNode{Val: 0, Next: head}",
                    "    groupPrev := dummy",
                    "    for {",
                    "        kth := getKth(groupPrev, k)",
                    "        if kth == nil {",
                    "            break",
                    "        }",
                    "        groupNext := kth.Next",
                    "        prev, curr := kth.Next, groupPrev.Next",
                    "        for curr != groupNext {",
                    "            tmp := curr.Next",
                    "            curr.Next = prev",
                    "            prev = curr",
                    "            curr = tmp",
                    "        }",
                    "        tmp := groupPrev.Next",
                    "        groupPrev.Next = kth",
                    "        groupPrev = tmp",
                    "    }",
                    "    return dummy.Next",
                    "}",
                    "func getKth(curr *ListNode, k int) *ListNode {",
                    "    for curr != nil && k > 0 {",
                    "        curr = curr.Next",
                    "        k--",
                    "    }",
                    "    return curr",
                    "}"
                  ]
                }
              ],
              "timeComplexity": "O(n)",
              "timeDescription": "Linear time as each node is visited once to find groups and once to reverse within groups",
              "spaceComplexity": "O(1)",
              "spaceDescription": "Constant extra space using only pointers for tracking, no recursion stack",
              "exampleWalkthrough": "Given head=[1,2,3,4,5], k=2: dummy→1→2→3→4→5. Find kth=2. Reverse group: groupPrev→2→1→groupNext(3). Update groupPrev to 1. Find kth=4. Reverse: 2→1→4→3→5. GroupPrev to 3. Find kth=None (only 1 node). Break. Result: 2→1→4→3→5.",
              "displayOrder": 2
            }
          ],
          "tags": [
            "linked-list",
            "recursion",
            "leetcode-hard"
          ],
          "nextReviewDate": null,
          "reviewCount": 0,
          "easeFactor": 2.5,
          "consecutiveCorrect": 0
        }
      ]
    }
  ]
}
