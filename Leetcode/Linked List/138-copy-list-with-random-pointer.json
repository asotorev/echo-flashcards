{
  "decks": [
    {
      "name": "LeetCode #138 - Copy List with Random Pointer",
      "description": "Create a deep copy of a linked list where each node has a next and random pointer",
      "cards": [
        {
          "id": "leetcode_138_copy_list_with_random_pointer",
          "question": "Copy List with Random Pointer: A linked list of length n is given such that each node contains an additional random pointer, which could point to any node in the list, or null. Construct a deep copy of the list. The deep copy should consist of exactly n brand new nodes, where each new node has its value set to the value of its corresponding original node. Both the next and random pointer of the new nodes should point to new nodes in the copied list such that the pointers in the original list and copied list represent the same list state. None of the pointers in the new list should point to nodes in the original list. Return the head of the copied linked list.",
          "solutions": [
            {
              "name": "Hash Map (Two Pass)",
              "type": "optimal",
              "explanation": "Use hash map to store mapping from original nodes to copied nodes. First pass creates all copy nodes and stores mappings. Second pass connects next and random pointers using the mapping. Most straightforward approach with clear separation of concerns.",
              "languageImplementations": [
                {
                  "language": "python",
                  "semanticLines": [
                    "Define function copyRandomList that accepts head of linked list with random pointers.",
                    "Create dictionary oldToCopy with None mapping to None to handle null pointers.",
                    "Initialize cur to head for first traversal.",
                    "Start while loop that continues while cur is not None to create all copy nodes.",
                    "Create new Node with cur dot val.",
                    "Store mapping from cur to copy in oldToCopy dictionary.",
                    "Move cur forward by setting cur to cur dot next.",
                    "Reset cur to head for second traversal.",
                    "Start while loop that continues while cur is not None to connect pointers.",
                    "Get copy node from oldToCopy using cur as key.",
                    "Set copy dot next to oldToCopy at cur dot next to connect next pointer.",
                    "Set copy dot random to oldToCopy at cur dot random to connect random pointer.",
                    "Move cur forward by setting cur to cur dot next.",
                    "Return oldToCopy at head as the copied list head."
                  ],
                  "codeLines": [
                    "def copyRandomList(head):",
                    "    oldToCopy = {None: None}",
                    "    cur = head",
                    "    while cur:",
                    "        copy = Node(cur.val)",
                    "        oldToCopy[cur] = copy",
                    "        cur = cur.next",
                    "    cur = head",
                    "    while cur:",
                    "        copy = oldToCopy[cur]",
                    "        copy.next = oldToCopy[cur.next]",
                    "        copy.random = oldToCopy[cur.random]",
                    "        cur = cur.next",
                    "    return oldToCopy[head]"
                  ]
                },
                {
                  "language": "javascript",
                  "semanticLines": [
                    "Define function copyRandomList that accepts head of linked list with random pointers.",
                    "Create Map oldToCopy to store mapping from original to copied nodes.",
                    "Set null mapping to null in oldToCopy to handle null pointers.",
                    "Declare cur initialized to head for first traversal.",
                    "Start while loop that continues while cur is not null to create all copy nodes.",
                    "Create new Node with cur dot val.",
                    "Store mapping from cur to copy in oldToCopy Map.",
                    "Move cur forward by setting cur to cur dot next.",
                    "Close the while loop with closing brace.",
                    "Reset cur to head for second traversal.",
                    "Start while loop that continues while cur is not null to connect pointers.",
                    "Get copy node from oldToCopy using cur as key.",
                    "Set copy dot next to oldToCopy get cur dot next to connect next pointer.",
                    "Set copy dot random to oldToCopy get cur dot random to connect random pointer.",
                    "Move cur forward by setting cur to cur dot next.",
                    "Close the while loop with closing brace.",
                    "Return oldToCopy get head as the copied list head.",
                    "Close function with closing brace."
                  ],
                  "codeLines": [
                    "function copyRandomList(head) {",
                    "    const oldToCopy = new Map();",
                    "    oldToCopy.set(null, null);",
                    "    let cur = head;",
                    "    while (cur) {",
                    "        const copy = new Node(cur.val);",
                    "        oldToCopy.set(cur, copy);",
                    "        cur = cur.next;",
                    "    }",
                    "    cur = head;",
                    "    while (cur) {",
                    "        const copy = oldToCopy.get(cur);",
                    "        copy.next = oldToCopy.get(cur.next);",
                    "        copy.random = oldToCopy.get(cur.random);",
                    "        cur = cur.next;",
                    "    }",
                    "    return oldToCopy.get(head);",
                    "}"
                  ]
                },
                {
                  "language": "go",
                  "semanticLines": [
                    "Define function copyRandomList that accepts head pointer to Node and returns pointer to Node.",
                    "Create map oldToCopy from pointer to Node to pointer to Node using make.",
                    "Set nil mapping to nil in oldToCopy to handle null pointers.",
                    "Initialize cur to head for first traversal.",
                    "Start for loop that continues while cur is not nil to create all copy nodes.",
                    "Create new Node with Val set to cur dot Val using address operator.",
                    "Store mapping from cur to copy in oldToCopy map.",
                    "Move cur forward by setting cur to cur dot Next.",
                    "Close the for loop with closing brace.",
                    "Reset cur to head for second traversal.",
                    "Start for loop that continues while cur is not nil to connect pointers.",
                    "Get copy node from oldToCopy using cur as key.",
                    "Set copy dot Next to oldToCopy at cur dot Next to connect next pointer.",
                    "Set copy dot Random to oldToCopy at cur dot Random to connect random pointer.",
                    "Move cur forward by setting cur to cur dot Next.",
                    "Close the for loop with closing brace.",
                    "Return oldToCopy at head as the copied list head.",
                    "Close function with closing brace."
                  ],
                  "codeLines": [
                    "func copyRandomList(head *Node) *Node {",
                    "    oldToCopy := make(map[*Node]*Node)",
                    "    oldToCopy[nil] = nil",
                    "    cur := head",
                    "    for cur != nil {",
                    "        copy := &Node{Val: cur.Val}",
                    "        oldToCopy[cur] = copy",
                    "        cur = cur.Next",
                    "    }",
                    "    cur = head",
                    "    for cur != nil {",
                    "        copy := oldToCopy[cur]",
                    "        copy.Next = oldToCopy[cur.Next]",
                    "        copy.Random = oldToCopy[cur.Random]",
                    "        cur = cur.Next",
                    "    }",
                    "    return oldToCopy[head]",
                    "}"
                  ]
                }
              ],
              "timeComplexity": "O(n)",
              "timeDescription": "Two passes through the list: first to create nodes, second to connect pointers",
              "spaceComplexity": "O(n)",
              "spaceDescription": "Hash map stores mapping for all n nodes plus output list of n nodes",
              "exampleWalkthrough": "Given list [[7,null],[13,0],[11,4],[10,2],[1,0]]: First pass creates 5 copy nodes with values. Second pass connects: node 13's random to node 7 copy, node 11's random to node 1 copy, etc.",
              "displayOrder": 1
            },
            {
              "name": "Hash Map (One Pass)",
              "type": "optimal",
              "explanation": "Use defaultdict to create nodes on-demand during single pass. When accessing a node that doesn't exist yet, it's automatically created. Elegant single-pass solution using lazy initialization.",
              "languageImplementations": [
                {
                  "language": "python",
                  "semanticLines": [
                    "Define function copyRandomList that accepts head of linked list with random pointers.",
                    "Import defaultdict from collections module.",
                    "Create defaultdict oldToCopy with lambda that returns new Node with value zero for on-demand node creation.",
                    "Set oldToCopy at None to None to handle null pointers.",
                    "Initialize cur to head for traversal.",
                    "Start while loop that continues while cur is not None.",
                    "Set oldToCopy at cur dot val to cur dot val to assign correct value.",
                    "Set oldToCopy at cur dot next to oldToCopy at cur dot next creating copy on-demand if needed.",
                    "Set oldToCopy at cur dot random to oldToCopy at cur dot random creating copy on-demand if needed.",
                    "Move cur forward by setting cur to cur dot next.",
                    "Return oldToCopy at head as the copied list head."
                  ],
                  "codeLines": [
                    "def copyRandomList(head):",
                    "    import collections",
                    "    oldToCopy = collections.defaultdict(lambda: Node(0))",
                    "    oldToCopy[None] = None",
                    "    cur = head",
                    "    while cur:",
                    "        oldToCopy[cur].val = cur.val",
                    "        oldToCopy[cur].next = oldToCopy[cur.next]",
                    "        oldToCopy[cur].random = oldToCopy[cur.random]",
                    "        cur = cur.next",
                    "    return oldToCopy[head]"
                  ]
                },
                {
                  "language": "javascript",
                  "semanticLines": [
                    "Define function copyRandomList that accepts head of linked list with random pointers.",
                    "Create Map oldToCopy to store mapping with lazy node creation.",
                    "Define helper function getOrCreate that accepts node parameter.",
                    "Check if node is null.",
                    "If null, return null.",
                    "Close the if block with closing brace.",
                    "Check if oldToCopy does not have node as key.",
                    "If not exists, set node mapping to new Node with value zero in oldToCopy.",
                    "Close the if block with closing brace.",
                    "Return oldToCopy get node.",
                    "Close the getOrCreate function with closing brace.",
                    "Declare cur initialized to head for traversal.",
                    "Start while loop that continues while cur is not null.",
                    "Get or create copy node using getOrCreate with cur.",
                    "Set copy dot val to cur dot val to assign correct value.",
                    "Set copy dot next to getOrCreate with cur dot next creating copy on-demand if needed.",
                    "Set copy dot random to getOrCreate with cur dot random creating copy on-demand if needed.",
                    "Move cur forward by setting cur to cur dot next.",
                    "Close the while loop with closing brace.",
                    "Return getOrCreate with head as the copied list head.",
                    "Close function with closing brace."
                  ],
                  "codeLines": [
                    "function copyRandomList(head) {",
                    "    const oldToCopy = new Map();",
                    "    function getOrCreate(node) {",
                    "        if (node === null) {",
                    "            return null;",
                    "        }",
                    "        if (!oldToCopy.has(node)) {",
                    "            oldToCopy.set(node, new Node(0));",
                    "        }",
                    "        return oldToCopy.get(node);",
                    "    }",
                    "    let cur = head;",
                    "    while (cur) {",
                    "        const copy = getOrCreate(cur);",
                    "        copy.val = cur.val;",
                    "        copy.next = getOrCreate(cur.next);",
                    "        copy.random = getOrCreate(cur.random);",
                    "        cur = cur.next;",
                    "    }",
                    "    return getOrCreate(head);",
                    "}"
                  ]
                },
                {
                  "language": "go",
                  "semanticLines": [
                    "Define function copyRandomList that accepts head pointer to Node and returns pointer to Node.",
                    "Create map oldToCopy from pointer to Node to pointer to Node using make.",
                    "Define helper function getOrCreate that accepts node pointer to Node and returns pointer to Node.",
                    "Check if node is nil.",
                    "If nil, return nil.",
                    "Close the if block with closing brace.",
                    "Declare ok as boolean.",
                    "Check if blank identifier and ok by accessing oldToCopy at node, negate ok.",
                    "If not exists, set node mapping to address of new Node with Val zero in oldToCopy.",
                    "Close the if block with closing brace.",
                    "Return oldToCopy at node.",
                    "Close the getOrCreate function with closing brace.",
                    "Initialize cur to head for traversal.",
                    "Start for loop that continues while cur is not nil.",
                    "Get or create copy node using getOrCreate with cur.",
                    "Set copy dot Val to cur dot Val to assign correct value.",
                    "Set copy dot Next to getOrCreate with cur dot Next creating copy on-demand if needed.",
                    "Set copy dot Random to getOrCreate with cur dot Random creating copy on-demand if needed.",
                    "Move cur forward by setting cur to cur dot Next.",
                    "Close the for loop with closing brace.",
                    "Return getOrCreate with head as the copied list head.",
                    "Close function with closing brace."
                  ],
                  "codeLines": [
                    "func copyRandomList(head *Node) *Node {",
                    "    oldToCopy := make(map[*Node]*Node)",
                    "    getOrCreate := func(node *Node) *Node {",
                    "        if node == nil {",
                    "            return nil",
                    "        }",
                    "        if _, ok := oldToCopy[node]; !ok {",
                    "            oldToCopy[node] = &Node{Val: 0}",
                    "        }",
                    "        return oldToCopy[node]",
                    "    }",
                    "    cur := head",
                    "    for cur != nil {",
                    "        copy := getOrCreate(cur)",
                    "        copy.Val = cur.Val",
                    "        copy.Next = getOrCreate(cur.Next)",
                    "        copy.Random = getOrCreate(cur.Random)",
                    "        cur = cur.Next",
                    "    }",
                    "    return getOrCreate(head)",
                    "}"
                  ]
                }
              ],
              "timeComplexity": "O(n)",
              "timeDescription": "Single pass through the list with lazy node creation",
              "spaceComplexity": "O(n)",
              "spaceDescription": "Hash map stores mapping for all n nodes plus output list of n nodes",
              "exampleWalkthrough": "Given list [[7,null],[13,0]]: Process node 7: create copy, next and random created lazily. Process node 13: create copy, random points to already-created node 7 copy.",
              "displayOrder": 2
            },
            {
              "name": "Space Optimized I (Interleaving)",
              "type": "optimal",
              "explanation": "Interleave copy nodes with original nodes in three passes: create and insert copies, set random pointers using original nodes, separate the two lists. Achieves O(1) extra space by using the list structure itself for mapping.",
              "languageImplementations": [
                {
                  "language": "python",
                  "semanticLines": [
                    "Define function copyRandomList that accepts head of linked list with random pointers.",
                    "Check if head is None.",
                    "If None, return None as empty list.",
                    "Initialize l1 to head for first pass to create and interleave copy nodes.",
                    "Start while loop that continues while l1 is not None.",
                    "Create new Node l2 with l1 dot val.",
                    "Set l2 dot next to l1 dot next to insert copy after original.",
                    "Set l1 dot next to l2 to link original to its copy.",
                    "Move l1 forward by setting l1 to l2 dot next to skip to next original node.",
                    "Store head dot next in newHead as the copied list head.",
                    "Reset l1 to head for second pass to set random pointers.",
                    "Start while loop that continues while l1 is not None.",
                    "Check if l1 dot random is not None.",
                    "If not None, set l1 dot next dot random to l1 dot random dot next to connect copy random to copy of original random.",
                    "Move l1 forward by setting l1 to l1 dot next dot next to skip copy nodes.",
                    "Reset l1 to head for third pass to separate lists.",
                    "Start while loop that continues while l1 is not None.",
                    "Store l1 dot next in l2 as current copy node.",
                    "Set l1 dot next to l2 dot next to restore original list.",
                    "Check if l2 dot next is not None.",
                    "If not None, set l2 dot next to l2 dot next dot next to link copies together.",
                    "Move l1 forward by setting l1 to l1 dot next.",
                    "Return newHead as the separated copied list."
                  ],
                  "codeLines": [
                    "def copyRandomList(head):",
                    "    if not head:",
                    "        return None",
                    "    l1 = head",
                    "    while l1:",
                    "        l2 = Node(l1.val)",
                    "        l2.next = l1.next",
                    "        l1.next = l2",
                    "        l1 = l2.next",
                    "    newHead = head.next",
                    "    l1 = head",
                    "    while l1:",
                    "        if l1.random:",
                    "            l1.next.random = l1.random.next",
                    "        l1 = l1.next.next",
                    "    l1 = head",
                    "    while l1:",
                    "        l2 = l1.next",
                    "        l1.next = l2.next",
                    "        if l2.next:",
                    "            l2.next = l2.next.next",
                    "        l1 = l1.next",
                    "    return newHead"
                  ]
                },
                {
                  "language": "javascript",
                  "semanticLines": [
                    "Define function copyRandomList that accepts head of linked list with random pointers.",
                    "Check if head is null.",
                    "If null, return null as empty list.",
                    "Close the if block with closing brace.",
                    "Declare l1 initialized to head for first pass to create and interleave copy nodes.",
                    "Start while loop that continues while l1 is not null.",
                    "Create new Node l2 with l1 dot val.",
                    "Set l2 dot next to l1 dot next to insert copy after original.",
                    "Set l1 dot next to l2 to link original to its copy.",
                    "Move l1 forward by setting l1 to l2 dot next to skip to next original node.",
                    "Close the while loop with closing brace.",
                    "Declare constant newHead as head dot next as the copied list head.",
                    "Reset l1 to head for second pass to set random pointers.",
                    "Start while loop that continues while l1 is not null.",
                    "Check if l1 dot random is not null.",
                    "If not null, set l1 dot next dot random to l1 dot random dot next to connect copy random to copy of original random.",
                    "Close the if block with closing brace.",
                    "Move l1 forward by setting l1 to l1 dot next dot next to skip copy nodes.",
                    "Close the while loop with closing brace.",
                    "Reset l1 to head for third pass to separate lists.",
                    "Start while loop that continues while l1 is not null.",
                    "Declare constant l2 as l1 dot next as current copy node.",
                    "Set l1 dot next to l2 dot next to restore original list.",
                    "Check if l2 dot next is not null.",
                    "If not null, set l2 dot next to l2 dot next dot next to link copies together.",
                    "Close the if block with closing brace.",
                    "Move l1 forward by setting l1 to l1 dot next.",
                    "Close the while loop with closing brace.",
                    "Return newHead as the separated copied list.",
                    "Close function with closing brace."
                  ],
                  "codeLines": [
                    "function copyRandomList(head) {",
                    "    if (!head) {",
                    "        return null;",
                    "    }",
                    "    let l1 = head;",
                    "    while (l1) {",
                    "        const l2 = new Node(l1.val);",
                    "        l2.next = l1.next;",
                    "        l1.next = l2;",
                    "        l1 = l2.next;",
                    "    }",
                    "    const newHead = head.next;",
                    "    l1 = head;",
                    "    while (l1) {",
                    "        if (l1.random) {",
                    "            l1.next.random = l1.random.next;",
                    "        }",
                    "        l1 = l1.next.next;",
                    "    }",
                    "    l1 = head;",
                    "    while (l1) {",
                    "        const l2 = l1.next;",
                    "        l1.next = l2.next;",
                    "        if (l2.next) {",
                    "            l2.next = l2.next.next;",
                    "        }",
                    "        l1 = l1.next;",
                    "    }",
                    "    return newHead;",
                    "}"
                  ]
                },
                {
                  "language": "go",
                  "semanticLines": [
                    "Define function copyRandomList that accepts head pointer to Node and returns pointer to Node.",
                    "Check if head is nil.",
                    "If nil, return nil as empty list.",
                    "Close the if block with closing brace.",
                    "Initialize l1 to head for first pass to create and interleave copy nodes.",
                    "Start for loop that continues while l1 is not nil.",
                    "Create new Node l2 with Val set to l1 dot Val using address operator.",
                    "Set l2 dot Next to l1 dot Next to insert copy after original.",
                    "Set l1 dot Next to l2 to link original to its copy.",
                    "Move l1 forward by setting l1 to l2 dot Next to skip to next original node.",
                    "Close the for loop with closing brace.",
                    "Store head dot Next in newHead as the copied list head.",
                    "Reset l1 to head for second pass to set random pointers.",
                    "Start for loop that continues while l1 is not nil.",
                    "Check if l1 dot Random is not nil.",
                    "If not nil, set l1 dot Next dot Random to l1 dot Random dot Next to connect copy random to copy of original random.",
                    "Close the if block with closing brace.",
                    "Move l1 forward by setting l1 to l1 dot Next dot Next to skip copy nodes.",
                    "Close the for loop with closing brace.",
                    "Reset l1 to head for third pass to separate lists.",
                    "Start for loop that continues while l1 is not nil.",
                    "Store l1 dot Next in l2 as current copy node.",
                    "Set l1 dot Next to l2 dot Next to restore original list.",
                    "Check if l2 dot Next is not nil.",
                    "If not nil, set l2 dot Next to l2 dot Next dot Next to link copies together.",
                    "Close the if block with closing brace.",
                    "Move l1 forward by setting l1 to l1 dot Next.",
                    "Close the for loop with closing brace.",
                    "Return newHead as the separated copied list.",
                    "Close function with closing brace."
                  ],
                  "codeLines": [
                    "func copyRandomList(head *Node) *Node {",
                    "    if head == nil {",
                    "        return nil",
                    "    }",
                    "    l1 := head",
                    "    for l1 != nil {",
                    "        l2 := &Node{Val: l1.Val}",
                    "        l2.Next = l1.Next",
                    "        l1.Next = l2",
                    "        l1 = l2.Next",
                    "    }",
                    "    newHead := head.Next",
                    "    l1 = head",
                    "    for l1 != nil {",
                    "        if l1.Random != nil {",
                    "            l1.Next.Random = l1.Random.Next",
                    "        }",
                    "        l1 = l1.Next.Next",
                    "    }",
                    "    l1 = head",
                    "    for l1 != nil {",
                    "        l2 := l1.Next",
                    "        l1.Next = l2.Next",
                    "        if l2.Next != nil {",
                    "            l2.Next = l2.Next.Next",
                    "        }",
                    "        l1 = l1.Next",
                    "    }",
                    "    return newHead",
                    "}"
                  ]
                }
              ],
              "timeComplexity": "O(n)",
              "timeDescription": "Three passes through the list: interleave, connect randoms, separate",
              "spaceComplexity": "O(1)",
              "spaceDescription": "O(1) extra space, O(n) for output list. No hash map needed by using list structure",
              "exampleWalkthrough": "Given 7→13: Pass 1: 7→7'→13→13'. Pass 2: If 13.random=7, then 13'.random=7'. Pass 3: Restore 7→13 and extract 7'→13'.",
              "displayOrder": 3
            },
            {
              "name": "Space Optimized II (Random Pointer Swap)",
              "type": "optimal",
              "explanation": "Temporarily use random pointers to store copies, then restore and build the new list. Alternative O(1) space approach that swaps random pointers for tracking.",
              "languageImplementations": [
                {
                  "language": "python",
                  "semanticLines": [
                    "Define function copyRandomList that accepts head of linked list with random pointers.",
                    "Check if head is None.",
                    "If None, return None as empty list.",
                    "Initialize l1 to head for first pass to create copies using random pointer storage.",
                    "Start while loop that continues while l1 is not None.",
                    "Create new Node l2 with l1 dot val.",
                    "Set l2 dot next to l1 dot random to temporarily store original random.",
                    "Set l1 dot random to l2 to store copy in random pointer.",
                    "Move l1 forward by setting l1 to l1 dot next.",
                    "Store head dot random in newHead as the copied list head.",
                    "Reset l1 to head for second pass to set random pointers in copies.",
                    "Start while loop that continues while l1 is not None.",
                    "Store l1 dot random in l2 as the copy node.",
                    "Check if l2 dot next is not None using ternary to set l2 dot random.",
                    "If l2 dot next exists, set l2 dot random to l2 dot next dot random, otherwise set to None.",
                    "Move l1 forward by setting l1 to l1 dot next.",
                    "Reset l1 to head for third pass to restore original randoms and connect copies.",
                    "Start while loop that continues while l1 is not None.",
                    "Store l1 dot random in l2 as the copy node.",
                    "Set l1 dot random to l2 dot next to restore original random pointer.",
                    "Check if l1 dot next is not None using ternary to set l2 dot next.",
                    "If l1 dot next exists, set l2 dot next to l1 dot next dot random, otherwise set to None.",
                    "Move l1 forward by setting l1 to l1 dot next.",
                    "Return newHead as the completed copied list."
                  ],
                  "codeLines": [
                    "def copyRandomList(head):",
                    "    if not head:",
                    "        return None",
                    "    l1 = head",
                    "    while l1:",
                    "        l2 = Node(l1.val)",
                    "        l2.next = l1.random",
                    "        l1.random = l2",
                    "        l1 = l1.next",
                    "    newHead = head.random",
                    "    l1 = head",
                    "    while l1:",
                    "        l2 = l1.random",
                    "        l2.random = l2.next.random if l2.next else None",
                    "        l1 = l1.next",
                    "    l1 = head",
                    "    while l1:",
                    "        l2 = l1.random",
                    "        l1.random = l2.next",
                    "        l2.next = l1.next.random if l1.next else None",
                    "        l1 = l1.next",
                    "    return newHead"
                  ]
                },
                {
                  "language": "javascript",
                  "semanticLines": [
                    "Define function copyRandomList that accepts head of linked list with random pointers.",
                    "Check if head is null.",
                    "If null, return null as empty list.",
                    "Close the if block with closing brace.",
                    "Declare l1 initialized to head for first pass to create copies using random pointer storage.",
                    "Start while loop that continues while l1 is not null.",
                    "Create new Node l2 with l1 dot val.",
                    "Set l2 dot next to l1 dot random to temporarily store original random.",
                    "Set l1 dot random to l2 to store copy in random pointer.",
                    "Move l1 forward by setting l1 to l1 dot next.",
                    "Close the while loop with closing brace.",
                    "Declare constant newHead as head dot random as the copied list head.",
                    "Reset l1 to head for second pass to set random pointers in copies.",
                    "Start while loop that continues while l1 is not null.",
                    "Declare constant l2 as l1 dot random as the copy node.",
                    "Use ternary to set l2 dot random checking if l2 dot next exists.",
                    "If l2 dot next exists, set l2 dot random to l2 dot next dot random, otherwise set to null.",
                    "Move l1 forward by setting l1 to l1 dot next.",
                    "Close the while loop with closing brace.",
                    "Reset l1 to head for third pass to restore original randoms and connect copies.",
                    "Start while loop that continues while l1 is not null.",
                    "Declare constant l2 as l1 dot random as the copy node.",
                    "Set l1 dot random to l2 dot next to restore original random pointer.",
                    "Use ternary to set l2 dot next checking if l1 dot next exists.",
                    "If l1 dot next exists, set l2 dot next to l1 dot next dot random, otherwise set to null.",
                    "Move l1 forward by setting l1 to l1 dot next.",
                    "Close the while loop with closing brace.",
                    "Return newHead as the completed copied list.",
                    "Close function with closing brace."
                  ],
                  "codeLines": [
                    "function copyRandomList(head) {",
                    "    if (!head) {",
                    "        return null;",
                    "    }",
                    "    let l1 = head;",
                    "    while (l1) {",
                    "        const l2 = new Node(l1.val);",
                    "        l2.next = l1.random;",
                    "        l1.random = l2;",
                    "        l1 = l1.next;",
                    "    }",
                    "    const newHead = head.random;",
                    "    l1 = head;",
                    "    while (l1) {",
                    "        const l2 = l1.random;",
                    "        l2.random = l2.next ? l2.next.random : null;",
                    "        l1 = l1.next;",
                    "    }",
                    "    l1 = head;",
                    "    while (l1) {",
                    "        const l2 = l1.random;",
                    "        l1.random = l2.next;",
                    "        l2.next = l1.next ? l1.next.random : null;",
                    "        l1 = l1.next;",
                    "    }",
                    "    return newHead;",
                    "}"
                  ]
                },
                {
                  "language": "go",
                  "semanticLines": [
                    "Define function copyRandomList that accepts head pointer to Node and returns pointer to Node.",
                    "Check if head is nil.",
                    "If nil, return nil as empty list.",
                    "Close the if block with closing brace.",
                    "Initialize l1 to head for first pass to create copies using random pointer storage.",
                    "Start for loop that continues while l1 is not nil.",
                    "Create new Node l2 with Val set to l1 dot Val using address operator.",
                    "Set l2 dot Next to l1 dot Random to temporarily store original random.",
                    "Set l1 dot Random to l2 to store copy in random pointer.",
                    "Move l1 forward by setting l1 to l1 dot Next.",
                    "Close the for loop with closing brace.",
                    "Store head dot Random in newHead as the copied list head.",
                    "Reset l1 to head for second pass to set random pointers in copies.",
                    "Start for loop that continues while l1 is not nil.",
                    "Store l1 dot Random in l2 as the copy node.",
                    "Check if l2 dot Next is not nil using if-else to set l2 dot Random.",
                    "If l2 dot Next exists, set l2 dot Random to l2 dot Next dot Random.",
                    "Close the inner if block with closing brace.",
                    "Otherwise set l2 dot Random to nil.",
                    "Close the else block with closing brace.",
                    "Move l1 forward by setting l1 to l1 dot Next.",
                    "Close the for loop with closing brace.",
                    "Reset l1 to head for third pass to restore original randoms and connect copies.",
                    "Start for loop that continues while l1 is not nil.",
                    "Store l1 dot Random in l2 as the copy node.",
                    "Set l1 dot Random to l2 dot Next to restore original random pointer.",
                    "Check if l1 dot Next is not nil using if-else to set l2 dot Next.",
                    "If l1 dot Next exists, set l2 dot Next to l1 dot Next dot Random.",
                    "Close the inner if block with closing brace.",
                    "Otherwise set l2 dot Next to nil.",
                    "Close the else block with closing brace.",
                    "Move l1 forward by setting l1 to l1 dot Next.",
                    "Close the for loop with closing brace.",
                    "Return newHead as the completed copied list.",
                    "Close function with closing brace."
                  ],
                  "codeLines": [
                    "func copyRandomList(head *Node) *Node {",
                    "    if head == nil {",
                    "        return nil",
                    "    }",
                    "    l1 := head",
                    "    for l1 != nil {",
                    "        l2 := &Node{Val: l1.Val}",
                    "        l2.Next = l1.Random",
                    "        l1.Random = l2",
                    "        l1 = l1.Next",
                    "    }",
                    "    newHead := head.Random",
                    "    l1 = head",
                    "    for l1 != nil {",
                    "        l2 := l1.Random",
                    "        if l2.Next != nil {",
                    "            l2.Random = l2.Next.Random",
                    "        } else {",
                    "            l2.Random = nil",
                    "        }",
                    "        l1 = l1.Next",
                    "    }",
                    "    l1 = head",
                    "    for l1 != nil {",
                    "        l2 := l1.Random",
                    "        l1.Random = l2.Next",
                    "        if l1.Next != nil {",
                    "            l2.Next = l1.Next.Random",
                    "        } else {",
                    "            l2.Next = nil",
                    "        }",
                    "        l1 = l1.Next",
                    "    }",
                    "    return newHead",
                    "}"
                  ]
                }
              ],
              "timeComplexity": "O(n)",
              "timeDescription": "Three passes: create copies in random pointers, connect randoms, restore and separate",
              "spaceComplexity": "O(1)",
              "spaceDescription": "O(1) extra space, O(n) for output. Uses existing random pointers for temporary storage",
              "exampleWalkthrough": "Given 7→13, 13.random=7: Pass 1: Store copies in randoms. Pass 2: Set copy randoms. Pass 3: Restore originals, link copies: 7'→13'.",
              "displayOrder": 4
            },
            {
              "name": "Recursion with Hash Map",
              "type": "alternative",
              "explanation": "Recursively create deep copy using hash map to track already-copied nodes. Base case returns None for null nodes or already-copied nodes from map. Elegant recursive approach but uses call stack.",
              "languageImplementations": [
                {
                  "language": "python",
                  "semanticLines": [
                    "Define class Solution.",
                    "Define constructor init method with self parameter.",
                    "Initialize self dot map as empty dictionary to track copied nodes.",
                    "Define method copyRandomList that accepts self and head.",
                    "Check if head is None.",
                    "If None, return None as base case.",
                    "Check if head is in self dot map.",
                    "If exists, return self dot map at head to avoid recopying.",
                    "Create new Node copy with head dot val.",
                    "Store mapping from head to copy in self dot map.",
                    "Recursively call copyRandomList with head dot next and assign to copy dot next.",
                    "Get copy dot random from self dot map using head dot random as key with get method.",
                    "Return copy as the completed node."
                  ],
                  "codeLines": [
                    "class Solution:",
                    "    def __init__(self):",
                    "        self.map = {}",
                    "    def copyRandomList(self, head):",
                    "        if not head:",
                    "            return None",
                    "        if head in self.map:",
                    "            return self.map[head]",
                    "        copy = Node(head.val)",
                    "        self.map[head] = copy",
                    "        copy.next = self.copyRandomList(head.next)",
                    "        copy.random = self.map.get(head.random)",
                    "        return copy"
                  ]
                },
                {
                  "language": "javascript",
                  "semanticLines": [
                    "Define class Solution with constructor.",
                    "Initialize this dot map as new Map to track copied nodes.",
                    "Close the constructor with closing brace.",
                    "Define method copyRandomList that accepts head.",
                    "Check if head is null.",
                    "If null, return null as base case.",
                    "Close the if block with closing brace.",
                    "Check if this dot map has head as key.",
                    "If exists, return this dot map get head to avoid recopying.",
                    "Close the if block with closing brace.",
                    "Create new Node copy with head dot val.",
                    "Store mapping from head to copy in this dot map.",
                    "Recursively call copyRandomList with head dot next and assign to copy dot next.",
                    "Get copy dot random from this dot map using head dot random as key with get method or null.",
                    "Return copy as the completed node.",
                    "Close the method with closing brace.",
                    "Close the class with closing brace."
                  ],
                  "codeLines": [
                    "class Solution {",
                    "    constructor() {",
                    "        this.map = new Map();",
                    "    }",
                    "    copyRandomList(head) {",
                    "        if (!head) {",
                    "            return null;",
                    "        }",
                    "        if (this.map.has(head)) {",
                    "            return this.map.get(head);",
                    "        }",
                    "        const copy = new Node(head.val);",
                    "        this.map.set(head, copy);",
                    "        copy.next = this.copyRandomList(head.next);",
                    "        copy.random = this.map.get(head.random) || null;",
                    "        return copy;",
                    "    }",
                    "}"
                  ]
                },
                {
                  "language": "go",
                  "semanticLines": [
                    "Define struct Solution with field m as map from pointer to Node to pointer to Node.",
                    "Define method copyRandomList with receiver s pointer to Solution that accepts head pointer to Node and returns pointer to Node.",
                    "Check if s dot m is nil.",
                    "If nil, initialize s dot m using make for map from pointer to Node to pointer to Node.",
                    "Close the if block with closing brace.",
                    "Check if head is nil.",
                    "If nil, return nil as base case.",
                    "Close the if block with closing brace.",
                    "Check if copy and ok by accessing s dot m at head, ok is true.",
                    "If exists, return copy to avoid recopying.",
                    "Close the if block with closing brace.",
                    "Create new Node copy with Val set to head dot Val using address operator.",
                    "Store mapping from head to copy in s dot m.",
                    "Recursively call copyRandomList with head dot Next and assign to copy dot Next.",
                    "Get copy dot Random from s dot m using head dot Random as key.",
                    "Return copy as the completed node.",
                    "Close the method with closing brace."
                  ],
                  "codeLines": [
                    "type Solution struct {",
                    "    m map[*Node]*Node",
                    "}",
                    "func (s *Solution) copyRandomList(head *Node) *Node {",
                    "    if s.m == nil {",
                    "        s.m = make(map[*Node]*Node)",
                    "    }",
                    "    if head == nil {",
                    "        return nil",
                    "    }",
                    "    if copy, ok := s.m[head]; ok {",
                    "        return copy",
                    "    }",
                    "    copy := &Node{Val: head.Val}",
                    "    s.m[head] = copy",
                    "    copy.Next = s.copyRandomList(head.Next)",
                    "    copy.Random = s.m[head.Random]",
                    "    return copy",
                    "}"
                  ]
                }
              ],
              "timeComplexity": "O(n)",
              "timeDescription": "Recursive traversal visits each node once with memoization preventing revisits",
              "spaceComplexity": "O(n)",
              "spaceDescription": "Hash map stores n nodes plus recursion call stack of depth n",
              "exampleWalkthrough": "Given 7→13, 13.random=7: copyRandomList(7): create 7', recurse for next. copyRandomList(13): create 13', map[7] exists for random. Return 7'.",
              "displayOrder": 5
            }
          ],
          "tags": [
            "linked-list",
            "hash-table",
            "recursion",
            "leetcode-medium"
          ],
          "nextReviewDate": null,
          "reviewCount": 0,
          "easeFactor": 2.5,
          "consecutiveCorrect": 0
        }
      ]
    }
  ]
}