{
  "decks": [
    {
      "name": "LeetCode #128 - Longest Consecutive Sequence",
      "description": "Find the length of the longest consecutive sequence in an unsorted array",
      "cards": [
        {
          "id": "leetcode_128_longest_consecutive",
          "question": "Longest Consecutive Sequence: Given an unsorted array of integers nums, return the length of the longest consecutive elements sequence. You must write an algorithm that runs in O(n) time.",
          "solutions": [
            {
              "name": "Hash Set Optimal",
              "type": "optimal",
              "explanation": "Store all numbers in a hash set. For each number, only start counting if it's the beginning of a sequence (num-1 not in set). Then count consecutive numbers forward.",
              "dryRun": {
                "example": "nums = [100, 4, 200, 1, 3, 2]",
                "trace": [
                  "numSet = {100, 4, 200, 1, 3, 2}",
                  "num=100 → 99 in set? No (sequence start!) → count: 100✓, 101? No → length=1 → longest=1",
                  "num=4 → 3 in set? Yes → SKIP (not a sequence start)",
                  "num=200 → 199 in set? No (sequence start!) → count: 200✓, 201? No → length=1 → longest=1",
                  "num=1 → 0 in set? No (sequence start!) → count: 1✓, 2✓, 3✓, 4✓, 5? No → length=4 → longest=4",
                  "num=3 → 2 in set? Yes → SKIP",
                  "num=2 → 1 in set? Yes → SKIP",
                  "return 4"
                ]
              },
              "mentalModel": {
                "keyInsight": "Only count from sequence STARTS — if num-1 exists, you're in the middle, skip it",
                "whyItWorks": "By only starting from sequence beginnings, each number is visited at most twice (once in loop, once in counting). This makes it O(n) instead of O(n²).",
                "visualization": "[1,2,3,4] and [100] and [200] are separate sequences. We only start counting from 1, 100, and 200."
              },
              "commonMistakes": [
                {
                  "wrong": "for num in nums:",
                  "right": "for num in numSet:",
                  "why": "Iterating over nums may process duplicates. Iterate over set to avoid redundant work."
                },
                {
                  "wrong": "if num - 1 not in nums:",
                  "right": "if (num - 1) not in numSet:",
                  "why": "Checking 'in nums' is O(n). Checking 'in numSet' is O(1). Must use the set!"
                },
                {
                  "wrong": "Starting count from every number",
                  "right": "Only start count when (num-1) not in set",
                  "why": "Without this optimization, algorithm becomes O(n²). The 'sequence start' check is the key to O(n)."
                },
                {
                  "wrong": "while (num + 1) in numSet: num += 1",
                  "right": "while (num + length) in numSet: length += 1",
                  "why": "Don't modify num — you need it to stay at sequence start. Use separate length counter."
                }
              ],
              "languageImplementations": [
                {
                  "language": "python",
                  "semanticLines": [
                    "Define function longestConsecutive(nums).",
                    "Create set from nums for O(1) lookups.",
                    "Initialize longest = 0 to track best result.",
                    "Loop through each num in the SET (not original array).",
                    "Check if (num-1) NOT in set — means num is a sequence START.",
                    "If sequence start: initialize length = 1.",
                    "While (num + length) exists in set...",
                    "...extend the sequence by incrementing length.",
                    "Update longest = max(longest, length).",
                    "Return longest."
                  ],
                  "codeLines": [
                    "def longestConsecutive(nums):",
                    "    numSet = set(nums)",
                    "    longest = 0",
                    "    for num in numSet:",
                    "        if (num - 1) not in numSet:",
                    "            length = 1",
                    "            while (num + length) in numSet:",
                    "                length += 1",
                    "            longest = max(longest, length)",
                    "    return longest"
                  ]
                },
                {
                  "language": "javascript",
                  "semanticLines": [
                    "Define function longestConsecutive(nums).",
                    "Create Set from nums for O(1) lookups.",
                    "Initialize longest = 0.",
                    "Loop through each num in the Set.",
                    "Check if (num-1) NOT in set — means sequence START.",
                    "If start: initialize length = 1.",
                    "While (num + length) exists in set...",
                    "...increment length.",
                    "",
                    "Update longest with Math.max.",
                    "",
                    "",
                    "Return longest.",
                    ""
                  ],
                  "codeLines": [
                    "function longestConsecutive(nums) {",
                    "    const numSet = new Set(nums);",
                    "    let longest = 0;",
                    "    for (const num of numSet) {",
                    "        if (!numSet.has(num - 1)) {",
                    "            let length = 1;",
                    "            while (numSet.has(num + length)) {",
                    "                length++;",
                    "            }",
                    "            longest = Math.max(longest, length);",
                    "        }",
                    "    }",
                    "    return longest;",
                    "}"
                  ]
                },
                {
                  "language": "go",
                  "semanticLines": [
                    "Define function longestConsecutive(nums []int) returning int.",
                    "Create map as set for O(1) lookups.",
                    "Populate set with all numbers.",
                    "",
                    "",
                    "Initialize longest = 0.",
                    "Loop through each num in set.",
                    "Check if (num-1) NOT in set — sequence start check.",
                    "If start: initialize length = 1.",
                    "Infinite loop to count consecutive numbers.",
                    "If (num+length) exists in set...",
                    "...increment length.",
                    "Else...",
                    "...break out of counting loop.",
                    "",
                    "",
                    "Update longest if current length is greater.",
                    "",
                    "",
                    "",
                    "",
                    "Return longest.",
                    ""
                  ],
                  "codeLines": [
                    "func longestConsecutive(nums []int) int {",
                    "    numSet := make(map[int]struct{})",
                    "    for _, num := range nums {",
                    "        numSet[num] = struct{}{}",
                    "    }",
                    "    longest := 0",
                    "    for num := range numSet {",
                    "        if _, found := numSet[num-1]; !found {",
                    "            length := 1",
                    "            for {",
                    "                if _, exists := numSet[num+length]; exists {",
                    "                    length++",
                    "                } else {",
                    "                    break",
                    "                }",
                    "            }",
                    "            if length > longest {",
                    "                longest = length",
                    "            }",
                    "        }",
                    "    }",
                    "    return longest",
                    "}"
                  ]
                }
              ],
              "timeComplexity": "O(n)",
              "timeDescription": "Each number visited at most twice: once in main loop, once during consecutive counting. The 'sequence start' check prevents redundant counting.",
              "spaceComplexity": "O(n)",
              "spaceDescription": "Hash set stores all unique numbers.",
              "displayOrder": 1
            },
            {
              "name": "Hash Map Union Find",
              "type": "optimal",
              "explanation": "Use hash map to track sequence lengths at boundaries. When adding a new number, check if neighbors exist and merge sequences by updating boundary lengths.",
              "dryRun": {
                "example": "nums = [1, 2, 3]",
                "trace": [
                  "mp = {}",
                  "num=1 → left=mp[0]=0, right=mp[2]=0 → length=0+0+1=1",
                  "  → mp[1]=1, mp[1-0]=mp[1]=1, mp[1+0]=mp[1]=1 → mp={1:1}",
                  "num=2 → left=mp[1]=1, right=mp[3]=0 → length=1+0+1=2",
                  "  → mp[2]=2, mp[2-1]=mp[1]=2, mp[2+0]=mp[2]=2 → mp={1:2, 2:2}",
                  "num=3 → left=mp[2]=2, right=mp[4]=0 → length=2+0+1=3",
                  "  → mp[3]=3, mp[3-2]=mp[1]=3, mp[3+0]=mp[3]=3 → mp={1:3, 2:2, 3:3}",
                  "Boundaries (1 and 3) both have length=3 → return 3"
                ]
              },
              "mentalModel": {
                "keyInsight": "Track sequence length at BOUNDARIES only — when merging, update both ends",
                "whyItWorks": "When you add a number, you only need to know how far the sequence extends left and right. The boundaries store the total length, middle values just mark 'visited'.",
                "visualization": "Sequence [1,2,3]: mp[1]=3 (left boundary), mp[3]=3 (right boundary), mp[2]=2 (middle, just marks visited)"
              },
              "commonMistakes": [
                {
                  "wrong": "Forgetting to check if num already in map",
                  "right": "if num in mp: continue",
                  "why": "Duplicates would incorrectly extend sequence lengths"
                },
                {
                  "wrong": "Only updating mp[num]",
                  "right": "Update mp[num], mp[num-left], and mp[num+right]",
                  "why": "Must update BOTH boundaries so future merges work correctly"
                },
                {
                  "wrong": "mp[num - 1] without default",
                  "right": "mp.get(num - 1, 0)",
                  "why": "If neighbor doesn't exist, we need 0, not KeyError"
                }
              ],
              "languageImplementations": [
                {
                  "language": "python",
                  "semanticLines": [
                    "Define function longestConsecutive(nums).",
                    "Create empty dict: {number → sequence_length_at_that_point}.",
                    "Initialize res = 0.",
                    "Loop through each num.",
                    "Skip if already processed (avoid duplicates).",
                    "",
                    "Get left neighbor's sequence length (0 if absent).",
                    "Get right neighbor's sequence length (0 if absent).",
                    "New merged length = left + right + 1.",
                    "Mark current number as processed with its length.",
                    "Update LEFT boundary of merged sequence.",
                    "Update RIGHT boundary of merged sequence.",
                    "Update res with max.",
                    "Return res."
                  ],
                  "codeLines": [
                    "def longestConsecutive(nums):",
                    "    mp = {}",
                    "    res = 0",
                    "    for num in nums:",
                    "        if num in mp:",
                    "            continue",
                    "        left = mp.get(num - 1, 0)",
                    "        right = mp.get(num + 1, 0)",
                    "        length = left + right + 1",
                    "        mp[num] = length",
                    "        mp[num - left] = length",
                    "        mp[num + right] = length",
                    "        res = max(res, length)",
                    "    return res"
                  ]
                },
                {
                  "language": "javascript",
                  "semanticLines": [
                    "Define function longestConsecutive(nums).",
                    "Create Map for sequence lengths.",
                    "Initialize res = 0.",
                    "Loop through each num.",
                    "Skip if already processed.",
                    "Get left neighbor's length (default 0).",
                    "Get right neighbor's length (default 0).",
                    "Calculate merged length = left + right + 1.",
                    "Mark current number with its length.",
                    "Update LEFT boundary.",
                    "Update RIGHT boundary.",
                    "Update res with Math.max.",
                    "",
                    "Return res.",
                    ""
                  ],
                  "codeLines": [
                    "function longestConsecutive(nums) {",
                    "    const mp = new Map();",
                    "    let res = 0;",
                    "    for (const num of nums) {",
                    "        if (mp.has(num)) continue;",
                    "        const left = mp.get(num - 1) || 0;",
                    "        const right = mp.get(num + 1) || 0;",
                    "        const length = left + right + 1;",
                    "        mp.set(num, length);",
                    "        mp.set(num - left, length);",
                    "        mp.set(num + right, length);",
                    "        res = Math.max(res, length);",
                    "    }",
                    "    return res;",
                    "}"
                  ]
                },
                {
                  "language": "go",
                  "semanticLines": [
                    "Define function longestConsecutive(nums []int) returning int.",
                    "Create map[int]int for sequence lengths.",
                    "Initialize res = 0.",
                    "Loop through each num.",
                    "Only process if num not yet seen (mp[num] == 0).",
                    "Get left neighbor's length.",
                    "Get right neighbor's length.",
                    "Calculate merged length = left + right + 1.",
                    "Mark current number with its length.",
                    "Update LEFT boundary.",
                    "Update RIGHT boundary.",
                    "Update res if current length is greater.",
                    "",
                    "",
                    "",
                    "",
                    "Return res.",
                    ""
                  ],
                  "codeLines": [
                    "func longestConsecutive(nums []int) int {",
                    "    mp := make(map[int]int)",
                    "    res := 0",
                    "    for _, num := range nums {",
                    "        if mp[num] == 0 {",
                    "            left := mp[num-1]",
                    "            right := mp[num+1]",
                    "            length := left + right + 1",
                    "            mp[num] = length",
                    "            mp[num-left] = length",
                    "            mp[num+right] = length",
                    "            if length > res {",
                    "                res = length",
                    "            }",
                    "        }",
                    "    }",
                    "    return res",
                    "}"
                  ]
                }
              ],
              "timeComplexity": "O(n)",
              "timeDescription": "Single pass through array. Each number processed exactly once with O(1) map operations.",
              "spaceComplexity": "O(n)",
              "spaceDescription": "Map stores entry for each unique number.",
              "displayOrder": 2
            },
            {
              "name": "Sorting",
              "type": "alternative",
              "explanation": "Sort the array first, then iterate through counting consecutive sequences while handling duplicates.",
              "dryRun": {
                "example": "nums = [100, 4, 200, 1, 3, 2]",
                "trace": [
                  "Sort → [1, 2, 3, 4, 100, 200]",
                  "curr=1, streak=0, i=0",
                  "i=0: nums[0]=1=curr → skip dups → streak=1, curr=2, i=1",
                  "i=1: nums[1]=2=curr → skip dups → streak=2, curr=3, i=2",
                  "i=2: nums[2]=3=curr → skip dups → streak=3, curr=4, i=3",
                  "i=3: nums[3]=4=curr → skip dups → streak=4, curr=5, i=4 → res=4",
                  "i=4: nums[4]=100≠curr(5) → reset: curr=100, streak=0 → streak=1, curr=101, i=5",
                  "i=5: nums[5]=200≠curr(101) → reset: curr=200, streak=0 → streak=1, curr=201, i=6",
                  "return 4"
                ]
              },
              "mentalModel": {
                "keyInsight": "After sorting, consecutive numbers are adjacent — just count them",
                "whyItWorks": "Sorting groups consecutive numbers together. Then a single scan finds the longest run.",
                "tradeoff": "Simpler logic but O(n log n) due to sorting. Doesn't meet O(n) requirement!"
              },
              "commonMistakes": [
                {
                  "wrong": "Not handling duplicates",
                  "right": "Skip duplicates with while loop before incrementing streak",
                  "why": "[1,1,2] should give length 2, not 3. Duplicates don't extend sequences."
                },
                {
                  "wrong": "Not checking empty array",
                  "right": "if len(nums) == 0: return 0",
                  "why": "Accessing nums[0] on empty array causes error"
                },
                {
                  "wrong": "nums.sort() in JavaScript",
                  "right": "nums.sort((a, b) => a - b)",
                  "why": "JS default sort is lexicographic: [1,10,2]. Must use numeric comparator."
                }
              ],
              "languageImplementations": [
                {
                  "language": "python",
                  "semanticLines": [
                    "Define function longestConsecutive(nums).",
                    "Handle empty array edge case.",
                    "",
                    "Sort the array in place.",
                    "Initialize res = 0.",
                    "Set curr to first element (expected next value).",
                    "Initialize streak = 0.",
                    "Initialize index i = 0.",
                    "Loop while i < length.",
                    "If current value doesn't match expected → gap found.",
                    "Reset curr to current value.",
                    "Reset streak to 0.",
                    "Skip duplicate values (inner while loop).",
                    "",
                    "Extend streak by 1.",
                    "Move expected value forward.",
                    "Update res with max.",
                    "Return res."
                  ],
                  "codeLines": [
                    "def longestConsecutive(nums):",
                    "    if len(nums) == 0:",
                    "        return 0",
                    "    nums.sort()",
                    "    res = 0",
                    "    curr = nums[0]",
                    "    streak = 0",
                    "    i = 0",
                    "    while i < len(nums):",
                    "        if curr != nums[i]:",
                    "            curr = nums[i]",
                    "            streak = 0",
                    "        while i < len(nums) and nums[i] == curr:",
                    "            i += 1",
                    "        streak += 1",
                    "        curr += 1",
                    "        res = max(res, streak)",
                    "    return res"
                  ]
                },
                {
                  "language": "javascript",
                  "semanticLines": [
                    "Define function longestConsecutive(nums).",
                    "Handle empty array edge case.",
                    "Sort with numeric comparator (crucial — JS default is lexicographic).",
                    "Initialize res, curr, streak, and index i.",
                    "Loop while i < length.",
                    "If current value doesn't match expected → gap found.",
                    "Reset curr to current value.",
                    "Reset streak to 0.",
                    "",
                    "Skip duplicate values (inner while loop).",
                    "Extend streak by 1.",
                    "Move expected value forward.",
                    "Update res with Math.max.",
                    "",
                    "Return res.",
                    ""
                  ],
                  "codeLines": [
                    "function longestConsecutive(nums) {",
                    "    if (nums.length === 0) return 0;",
                    "    nums.sort((a, b) => a - b);",
                    "    let res = 0, curr = nums[0], streak = 0, i = 0;",
                    "    while (i < nums.length) {",
                    "        if (curr !== nums[i]) {",
                    "            curr = nums[i];",
                    "            streak = 0;",
                    "        }",
                    "        while (i < nums.length && nums[i] === curr) i++;",
                    "        streak++;",
                    "        curr++;",
                    "        res = Math.max(res, streak);",
                    "    }",
                    "    return res;",
                    "}"
                  ]
                },
                {
                  "language": "go",
                  "semanticLines": [
                    "Define function longestConsecutive(nums []int) returning int.",
                    "Handle empty slice.",
                    "",
                    "",
                    "Sort with sort.Ints.",
                    "Initialize res, curr, streak, and index i.",
                    "Loop while i < length.",
                    "If current value doesn't match expected → gap found.",
                    "Reset curr to current value.",
                    "Reset streak to 0.",
                    "",
                    "Skip duplicate values (inner for loop).",
                    "",
                    "",
                    "Extend streak by 1.",
                    "Move expected value forward.",
                    "Update res if current streak is greater.",
                    "",
                    "",
                    "",
                    "Return res.",
                    ""
                  ],
                  "codeLines": [
                    "func longestConsecutive(nums []int) int {",
                    "    if len(nums) == 0 {",
                    "        return 0",
                    "    }",
                    "    sort.Ints(nums)",
                    "    res, curr, streak, i := 0, nums[0], 0, 0",
                    "    for i < len(nums) {",
                    "        if curr != nums[i] {",
                    "            curr = nums[i]",
                    "            streak = 0",
                    "        }",
                    "        for i < len(nums) && nums[i] == curr {",
                    "            i++",
                    "        }",
                    "        streak++",
                    "        curr++",
                    "        if streak > res {",
                    "            res = streak",
                    "        }",
                    "    }",
                    "    return res",
                    "}"
                  ]
                }
              ],
              "timeComplexity": "O(n log n)",
              "timeDescription": "Dominated by sorting. Linear scan after is O(n).",
              "spaceComplexity": "O(1) or O(n)",
              "spaceDescription": "Depends on sorting algorithm. In-place sort is O(1), otherwise O(n).",
              "displayOrder": 3
            },
            {
              "name": "Brute Force",
              "type": "naive",
              "explanation": "For each number, try to build the longest consecutive sequence starting from it. No optimization to skip non-starts.",
              "dryRun": {
                "example": "nums = [1, 2, 3, 4]",
                "trace": [
                  "store = {1, 2, 3, 4}",
                  "num=1 → count: 1✓, 2✓, 3✓, 4✓, 5✗ → streak=4 → res=4",
                  "num=2 → count: 2✓, 3✓, 4✓, 5✗ → streak=3 → res=4",
                  "num=3 → count: 3✓, 4✓, 5✗ → streak=2 → res=4",
                  "num=4 → count: 4✓, 5✗ → streak=1 → res=4",
                  "Total operations: 4+3+2+1 = 10 (O(n²) work!)",
                  "return 4"
                ]
              },
              "mentalModel": {
                "keyInsight": "Try building sequence from EVERY number (wasteful!)",
                "whyItWorks": "Exhaustive search guarantees finding the answer, but does redundant work.",
                "whyItsWasteful": "Numbers 2,3,4 are all part of the sequence starting at 1. Counting from them is redundant."
              },
              "commonMistakes": [
                {
                  "wrong": "This is the optimal solution",
                  "right": "This is O(n²), not O(n)",
                  "why": "Without the 'sequence start' check, every number triggers a full count. Worst case: [1,2,3,...,n] does n+(n-1)+...+1 = O(n²) work."
                }
              ],
              "languageImplementations": [
                {
                  "language": "python",
                  "semanticLines": [
                    "Define function longestConsecutive(nums).",
                    "Initialize res = 0.",
                    "Create set for O(1) lookups.",
                    "Loop through each num (NO 'sequence start' optimization).",
                    "Initialize streak=0 and curr=num.",
                    "While curr exists in set, count consecutive.",
                    "",
                    "",
                    "Update res with max streak.",
                    "Return res."
                  ],
                  "codeLines": [
                    "def longestConsecutive(nums):",
                    "    res = 0",
                    "    store = set(nums)",
                    "    for num in nums:",
                    "        streak, curr = 0, num",
                    "        while curr in store:",
                    "            streak += 1",
                    "            curr += 1",
                    "        res = max(res, streak)",
                    "    return res"
                  ]
                },
                {
                  "language": "javascript",
                  "semanticLines": [
                    "Define function longestConsecutive(nums).",
                    "Initialize res = 0.",
                    "Create Set for O(1) lookups.",
                    "Loop through each num (no skip optimization).",
                    "Initialize streak=0 and curr=num.",
                    "While curr exists in set...",
                    "...increment streak.",
                    "...increment curr.",
                    "",
                    "Update res with Math.max.",
                    "",
                    "Return res.",
                    ""
                  ],
                  "codeLines": [
                    "function longestConsecutive(nums) {",
                    "    let res = 0;",
                    "    const store = new Set(nums);",
                    "    for (const num of nums) {",
                    "        let streak = 0, curr = num;",
                    "        while (store.has(curr)) {",
                    "            streak++;",
                    "            curr++;",
                    "        }",
                    "        res = Math.max(res, streak);",
                    "    }",
                    "    return res;",
                    "}"
                  ]
                },
                {
                  "language": "go",
                  "semanticLines": [
                    "Define function longestConsecutive(nums []int) returning int.",
                    "Initialize res = 0.",
                    "Create map as set.",
                    "Populate set with all numbers.",
                    "",
                    "",
                    "Loop through each num (no skip optimization).",
                    "Initialize streak=0 and curr=num.",
                    "Infinite loop to count consecutive.",
                    "If curr exists in set...",
                    "...increment streak.",
                    "...increment curr.",
                    "Else...",
                    "...break.",
                    "",
                    "",
                    "Update res if streak is greater.",
                    "",
                    "",
                    "",
                    "Return res.",
                    ""
                  ],
                  "codeLines": [
                    "func longestConsecutive(nums []int) int {",
                    "    res := 0",
                    "    store := make(map[int]struct{})",
                    "    for _, num := range nums {",
                    "        store[num] = struct{}{}",
                    "    }",
                    "    for _, num := range nums {",
                    "        streak, curr := 0, num",
                    "        for {",
                    "            if _, ok := store[curr]; ok {",
                    "                streak++",
                    "                curr++",
                    "            } else {",
                    "                break",
                    "            }",
                    "        }",
                    "        if streak > res {",
                    "            res = streak",
                    "        }",
                    "    }",
                    "    return res",
                    "}"
                  ]
                }
              ],
              "timeComplexity": "O(n²)",
              "timeDescription": "Worst case: sequence [1,2,...,n] does n+(n-1)+...+1 = n(n+1)/2 = O(n²) lookups.",
              "spaceComplexity": "O(n)",
              "spaceDescription": "Set stores all unique numbers.",
              "displayOrder": 4
            }
          ],
          "tags": [
            "arrays",
            "hash-set",
            "hash-map",
            "union-find",
            "leetcode-medium"
          ],
          "nextReviewDate": null,
          "reviewCount": 0,
          "easeFactor": 2.5,
          "consecutiveCorrect": 0
        }
      ]
    }
  ]
}