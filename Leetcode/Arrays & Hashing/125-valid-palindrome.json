{
  "decks": [
    {
      "name": "LeetCode #125 - Valid Palindrome",
      "description": "Determine if a string is a palindrome after converting to lowercase and removing non-alphanumeric characters",
      "cards": [
        {
          "id": "leetcode_125_valid_palindrome",
          "question": "Valid Palindrome: Given a string s, return true if it is a palindrome, false otherwise. A phrase is a palindrome if, after converting all uppercase letters into lowercase letters and removing all non-alphanumeric characters, it reads the same forward and backward. Alphanumeric characters include letters and numbers.",
          "solutions": [
            {
              "name": "Two Pointers",
              "type": "optimal",
              "explanation": "Use two pointers from the start and end of the string. Skip non-alphanumeric characters and compare the alphanumeric characters case-insensitively. This approach uses constant extra space by processing the string in-place without creating new strings.",
              "languageImplementations": [
                {
                  "language": "python",
                  "semanticLines": [
                    "Define function isPalindrome that accepts string s",
                    "Initialize variable l to zero for left pointer and variable r to length of s minus one for right pointer",
                    "Create empty line",
                    "Loop while l is less than r",
                    "Loop while l is less than r and character s at l is not alphanumeric",
                    "Increment l by one",
                    "Loop while l is less than r and character s at r is not alphanumeric",
                    "Decrement r by one",
                    "Get character s at l, convert to lowercase, get character s at r, convert to lowercase, compare if not equal",
                    "If not equal, return False",
                    "Increment l by one",
                    "Decrement r by one",
                    "After all comparisons, return True"
                  ],
                  "codeLines": [
                    "def isPalindrome(s):",
                    "    l, r = 0, len(s) - 1",
                    "    ",
                    "    while l < r:",
                    "        while l < r and not s[l].isalnum():",
                    "            l += 1",
                    "        while l < r and not s[r].isalnum():",
                    "            r -= 1",
                    "        if s[l].lower() != s[r].lower():",
                    "            return False",
                    "        l += 1",
                    "        r -= 1",
                    "    return True"
                  ]
                },
                {
                  "language": "javascript",
                  "semanticLines": [
                    "Define function isPalindrome that accepts string s",
                    "Initialize variable l to zero for left pointer and variable r to length of s minus one for right pointer",
                    "Create empty line",
                    "Loop while l is less than r",
                    "Loop while l is less than r and isAlphaNum called with character s at l returns false",
                    "Increment l by one",
                    "Close the inner while loop with closing brace",
                    "Loop while l is less than r and isAlphaNum called with character s at r returns false",
                    "Decrement r by one",
                    "Close the inner while loop with closing brace",
                    "Get character s at l, convert to lowercase, get character s at r, convert to lowercase, compare if not equal",
                    "If not equal, return false",
                    "Close the if statement with closing brace",
                    "Increment l by one",
                    "Decrement r by one",
                    "Close the outer while loop with closing brace",
                    "After all comparisons, return true",
                    "Close the function with closing brace",
                    "Create empty line",
                    "Define function isAlphaNum that accepts character c",
                    "Test character c against regular expression that matches letters and digits, and return the result",
                    "Close the function with closing brace"
                  ],
                  "codeLines": [
                    "function isPalindrome(s) {",
                    "    let l = 0, r = s.length - 1;",
                    "    ",
                    "    while (l < r) {",
                    "        while (l < r && !isAlphaNum(s[l])) {",
                    "            l++;",
                    "        }",
                    "        while (l < r && !isAlphaNum(s[r])) {",
                    "            r--;",
                    "        }",
                    "        if (s[l].toLowerCase() !== s[r].toLowerCase()) {",
                    "            return false;",
                    "        }",
                    "        l++;",
                    "        r--;",
                    "    }",
                    "    return true;",
                    "}",
                    "",
                    "function isAlphaNum(c) {",
                    "    return /[a-zA-Z0-9]/.test(c);",
                    "}"
                  ]
                },
                {
                  "language": "go",
                  "semanticLines": [
                    "Define function isPalindrome that accepts string s and returns bool",
                    "Initialize variable l to zero for left pointer and variable r to length of s minus one for right pointer",
                    "Create empty line",
                    "Loop while l is less than r using for loop",
                    "Loop while l is less than r and isAlphaNum called with rune of s at l returns false",
                    "Increment l by one",
                    "Close the inner for loop with closing brace",
                    "Loop while r is greater than l and isAlphaNum called with rune of s at r returns false",
                    "Decrement r by one",
                    "Close the inner for loop with closing brace",
                    "Get character s at l, convert to rune, convert to lowercase using unicode ToLower, get character s at r, convert to rune, convert to lowercase using unicode ToLower, compare if not equal",
                    "If not equal, return false",
                    "Close the if block with closing brace",
                    "Increment l by one",
                    "Decrement r by one",
                    "Close the outer for loop with closing brace",
                    "After all comparisons, return true",
                    "Close the function with closing brace",
                    "Create empty line",
                    "Define function isAlphaNum that accepts rune c and returns bool",
                    "Check if c is a letter using unicode IsLetter or c is a digit using unicode IsDigit, and return the result",
                    "Close the function with closing brace"
                  ],
                  "codeLines": [
                    "func isPalindrome(s string) bool {",
                    "    l, r := 0, len(s)-1",
                    "",
                    "    for l < r {",
                    "        for l < r && !isAlphaNum(rune(s[l])) {",
                    "            l++",
                    "        }",
                    "        for r > l && !isAlphaNum(rune(s[r])) {",
                    "            r--",
                    "        }",
                    "        if unicode.ToLower(rune(s[l])) != unicode.ToLower(rune(s[r])) {",
                    "            return false",
                    "        }",
                    "        l++",
                    "        r--",
                    "    }",
                    "    return true",
                    "}",
                    "",
                    "func isAlphaNum(c rune) bool {",
                    "    return unicode.IsLetter(c) || unicode.IsDigit(c)",
                    "}"
                  ]
                }
              ],
              "timeComplexity": "O(n)",
              "timeDescription": "Linear time where n is the length of the string. We traverse at most half the string with two pointers, visiting each character once.",
              "spaceComplexity": "O(1)",
              "spaceDescription": "Constant extra space. We only use two pointer variables regardless of input size.",
              "exampleWalkthrough": "For 'A man, a plan, a canal: Panama': Start with l=0, r=29. Skip non-alphanumeric: l=0('A'), r=20('a'). Compare 'a'=='a', move inward. Continue comparing pairs: 'm'=='m', 'a'=='a', 'n'=='n', etc. All pairs match, return true.",
              "displayOrder": 1
            },
            {
              "name": "Reverse String",
              "type": "alternative",
              "explanation": "Build a new string containing only lowercase alphanumeric characters, then reverse it and compare with the original processed string. Simple but uses extra space for the new strings.",
              "languageImplementations": [
                {
                  "language": "python",
                  "semanticLines": [
                    "Define function isPalindrome that accepts string s",
                    "Initialize variable newStr to empty string",
                    "Loop through each character c in string s",
                    "Check if character c is alphanumeric",
                    "Get character c, convert to lowercase, concatenate to newStr, and store back in newStr",
                    "Compare newStr with newStr reversed using slice notation, and return the result"
                  ],
                  "codeLines": [
                    "def isPalindrome(s):",
                    "    newStr = \"\"",
                    "    for c in s:",
                    "        if c.isalnum():",
                    "            newStr += c.lower()",
                    "    return newStr == newStr[::-1]"
                  ]
                },
                {
                  "language": "javascript",
                  "semanticLines": [
                    "Define function isPalindrome that accepts string s",
                    "Initialize variable newStr to empty string",
                    "Loop through each character c in string s",
                    "Test character c against regular expression that matches letters and digits",
                    "Get character c, convert to lowercase, concatenate to newStr, and store back in newStr",
                    "Close the if statement with closing brace",
                    "Close the for loop with closing brace",
                    "Split newStr into array of characters, reverse the array, join back into string, compare with newStr, and return the result",
                    "Close the function with closing brace"
                  ],
                  "codeLines": [
                    "function isPalindrome(s) {",
                    "    let newStr = \"\";",
                    "    for (const c of s) {",
                    "        if (/[a-zA-Z0-9]/.test(c)) {",
                    "            newStr += c.toLowerCase();",
                    "        }",
                    "    }",
                    "    return newStr === newStr.split('').reverse().join('');",
                    "}"
                  ]
                },
                {
                  "language": "go",
                  "semanticLines": [
                    "Define function isPalindrome that accepts string s and returns bool",
                    "Initialize variable newStr to empty string",
                    "Loop through string s using range to get each character c ignoring the index",
                    "Check if c is between lowercase a and z or c is between digit zero and nine",
                    "Convert character c to string, concatenate to newStr, and store back in newStr",
                    "Otherwise check if c is between uppercase A and Z",
                    "Get character c, add the difference between lowercase a and uppercase A to convert to lowercase, convert to string, concatenate to newStr, and store back in newStr",
                    "Close the else if block with closing brace",
                    "Close the for loop with closing brace",
                    "Create empty line",
                    "Call reverse function with newStr and store result in reversedStr",
                    "Compare newStr with reversedStr and return the result",
                    "Close the function with closing brace",
                    "Create empty line",
                    "Define function reverse that accepts string s and returns string",
                    "Convert string s to slice of runes and assign to runes",
                    "Get length of runes and assign to n",
                    "Loop with i from zero while i is less than n divided by two incrementing i",
                    "Swap runes at i with runes at n minus one minus i",
                    "Close the for loop with closing brace",
                    "Convert runes back to string and return it",
                    "Close the function with closing brace"
                  ],
                  "codeLines": [
                    "func isPalindrome(s string) bool {",
                    "    newStr := \"\"",
                    "    for _, c := range s {",
                    "        if ('a' <= c && c <= 'z') || ('0' <= c && c <= '9') {",
                    "            newStr += string(c)",
                    "        } else if 'A' <= c && c <= 'Z' {",
                    "            newStr += string(c + 'a' - 'A')",
                    "        }",
                    "    }",
                    "",
                    "    reversedStr := reverse(newStr)",
                    "    return newStr == reversedStr",
                    "}",
                    "",
                    "func reverse(s string) string {",
                    "    runes := []rune(s)",
                    "    n := len(runes)",
                    "    for i := 0; i < n/2; i++ {",
                    "        runes[i], runes[n-1-i] = runes[n-1-i], runes[i]",
                    "    }",
                    "    return string(runes)",
                    "}"
                  ]
                }
              ],
              "timeComplexity": "O(n)",
              "timeDescription": "Linear time where n is the length of the string. We iterate through the string once to build the filtered string, and once more to reverse it.",
              "spaceComplexity": "O(n)",
              "spaceDescription": "Linear space for storing the filtered and reversed strings. In the worst case where all characters are alphanumeric, we store two strings of length n.",
              "exampleWalkthrough": "For 'A man, a plan, a canal: Panama': Build newStr='amanaplanacanalpanama' by filtering and lowercasing. Reverse to get 'amanaplanacanalpanama'. Compare: they are equal, return true.",
              "displayOrder": 2
            }
          ],
          "tags": [
            "strings",
            "two-pointers",
            "palindrome",
            "leetcode-easy"
          ],
          "nextReviewDate": null,
          "reviewCount": 0,
          "easeFactor": 2.5,
          "consecutiveCorrect": 0
        }
      ]
    }
  ]
}
