{
  "decks": [
    {
      "name": "LeetCode #981 - Time Based Key-Value Store",
      "description": "Design a time-based key-value data structure with set and get operations",
      "cards": [
        {
          "id": "leetcode_981_time_based_key_value_store",
          "question": "Time Based Key-Value Store: Design a time-based key-value data structure that can store multiple values for the same key at different time stamps and retrieve the key's value at a certain timestamp. Implement the TimeMap class: TimeMap() Initializes the object of the data structure. void set(String key, String value, int timestamp) Stores the key with the value at the given time timestamp. String get(String key, int timestamp) Returns a value such that set was called previously with timestamp_prev <= timestamp. If there are multiple such values, it returns the value associated with the largest timestamp_prev. If there are no values, it returns an empty string. All timestamps in all set operations are strictly increasing.",
          "solutions": [
            {
              "name": "Binary Search (Array)",
              "type": "optimal",
              "explanation": "Store key-value-timestamp pairs in a map where each key maps to an array of pairs sorted by timestamp. Use custom binary search to find the largest timestamp less than or equal to query timestamp. Most efficient approach with O(1) set and O(log n) get.",
              "languageImplementations": [
                {
                  "language": "python",
                  "semanticLines": [
                    "Define class TimeMap.",
                    "Define constructor init method with self parameter.",
                    "Initialize self dot m as empty dictionary to store key to list of timestamp-value pairs.",
                    "Define method set that accepts self, key, value, and timestamp.",
                    "Check if key is not in self dot m.",
                    "If not exists, initialize self dot m at key as empty list.",
                    "Append tuple of timestamp and value to self dot m at key.",
                    "Define method get that accepts self, key, and timestamp.",
                    "Check if key is not in self dot m.",
                    "If not exists, return empty string.",
                    "Store self dot m at key in variable pairs.",
                    "Initialize l to zero and r to length of pairs minus one.",
                    "Start while loop that continues while l is less than or equal to r.",
                    "Calculate midpoint mid by adding l to r and dividing by two using integer division.",
                    "Check if timestamp at index mid in pairs is less than or equal to timestamp.",
                    "If less or equal, check if mid equals length of pairs minus one or timestamp at mid plus one is greater than timestamp.",
                    "If either condition true, return value at index mid in pairs as closest match found.",
                    "Set l to mid plus one to search right half for later timestamp.",
                    "Otherwise timestamp at mid is greater than timestamp.",
                    "Set r to mid minus one to search left half.",
                    "After loop ends, return empty string indicating no valid timestamp found."
                  ],
                  "codeLines": [
                    "class TimeMap:",
                    "    def __init__(self):",
                    "        self.m = {}",
                    "    def set(self, key, value, timestamp):",
                    "        if key not in self.m:",
                    "            self.m[key] = []",
                    "        self.m[key].append((timestamp, value))",
                    "    def get(self, key, timestamp):",
                    "        if key not in self.m:",
                    "            return \"\"",
                    "        pairs = self.m[key]",
                    "        l, r = 0, len(pairs) - 1",
                    "        while l <= r:",
                    "            mid = (l + r) // 2",
                    "            if pairs[mid][0] <= timestamp:",
                    "                if mid == len(pairs) - 1 or pairs[mid + 1][0] > timestamp:",
                    "                    return pairs[mid][1]",
                    "                l = mid + 1",
                    "            else:",
                    "                r = mid - 1",
                    "        return \"\""
                  ]
                },
                {
                  "language": "javascript",
                  "semanticLines": [
                    "Define class TimeMap with constructor function.",
                    "Initialize this dot m as empty object to store key to array of timestamp-value pairs.",
                    "Close the constructor with closing brace.",
                    "Define method set that accepts key, value, and timestamp.",
                    "Check if this dot m at key is undefined.",
                    "If undefined, initialize this dot m at key as empty array.",
                    "Close the if block with closing brace.",
                    "Push object with timestamp and value properties to this dot m at key.",
                    "Close the set method with closing brace.",
                    "Define method get that accepts key and timestamp.",
                    "Check if this dot m at key is undefined.",
                    "If undefined, return empty string.",
                    "Close the if block with closing brace.",
                    "Store this dot m at key in constant pairs.",
                    "Declare l initialized to zero and r initialized to length of pairs minus one.",
                    "Start while loop that continues while l is less than or equal to r.",
                    "Declare constant mid by floor dividing l plus r by two.",
                    "Check if timestamp property of pairs at index mid is less than or equal to timestamp.",
                    "If less or equal, check if mid equals length of pairs minus one or timestamp of pairs at mid plus one is greater than timestamp.",
                    "If either condition true, return value property of pairs at mid as closest match found.",
                    "Close the inner if block with closing brace.",
                    "Set l to mid plus one to search right half for later timestamp.",
                    "Close the if block with closing brace.",
                    "Otherwise timestamp at mid is greater than timestamp.",
                    "Set r to mid minus one to search left half.",
                    "Close the else block with closing brace.",
                    "Close the while loop with closing brace.",
                    "After loop ends, return empty string indicating no valid timestamp found.",
                    "Close the get method with closing brace."
                  ],
                  "codeLines": [
                    "class TimeMap {",
                    "    constructor() {",
                    "        this.m = {};",
                    "    }",
                    "    set(key, value, timestamp) {",
                    "        if (this.m[key] === undefined) {",
                    "            this.m[key] = [];",
                    "        }",
                    "        this.m[key].push({ timestamp, value });",
                    "    }",
                    "    get(key, timestamp) {",
                    "        if (this.m[key] === undefined) {",
                    "            return \"\";",
                    "        }",
                    "        const pairs = this.m[key];",
                    "        let l = 0, r = pairs.length - 1;",
                    "        while (l <= r) {",
                    "            const mid = Math.floor((l + r) / 2);",
                    "            if (pairs[mid].timestamp <= timestamp) {",
                    "                if (mid === pairs.length - 1 || pairs[mid + 1].timestamp > timestamp) {",
                    "                    return pairs[mid].value;",
                    "                }",
                    "                l = mid + 1;",
                    "            } else {",
                    "                r = mid - 1;",
                    "            }",
                    "        }",
                    "        return \"\";",
                    "    }",
                    "}"
                  ]
                },
                {
                  "language": "go",
                  "semanticLines": [
                    "Define struct pair with timestamp as int and value as string.",
                    "Define struct TimeMap with field m as map from string to slice of pair.",
                    "Define function Constructor that returns TimeMap.",
                    "Return TimeMap with m initialized using make for map from string to slice of pair.",
                    "Close the Constructor function with closing brace.",
                    "Define method Set with receiver this pointer to TimeMap that accepts key string, value string, and timestamp int.",
                    "Append new pair with timestamp and value to this dot m at key.",
                    "Close the Set method with closing brace.",
                    "Define method Get with receiver this pointer to TimeMap that accepts key string and timestamp int and returns string.",
                    "Check if blank identifier and exists by accessing this dot m at key, negate exists.",
                    "If not exists, return empty string.",
                    "Close the if block with closing brace.",
                    "Store this dot m at key in variable pairs.",
                    "Initialize l to zero and r to length of pairs minus one.",
                    "Start for loop that continues while l is less than or equal to r.",
                    "Calculate midpoint mid by adding l to r and dividing by two using integer division.",
                    "Check if timestamp field of pairs at index mid is less than or equal to timestamp.",
                    "If less or equal, check if mid equals length of pairs minus one or timestamp of pairs at mid plus one is greater than timestamp.",
                    "If either condition true, return value field of pairs at mid as closest match found.",
                    "Close the inner if block with closing brace.",
                    "Set l to mid plus one to search right half for later timestamp.",
                    "Close the if block with closing brace.",
                    "Otherwise timestamp at mid is greater than timestamp.",
                    "Set r to mid minus one to search left half.",
                    "Close the else block with closing brace.",
                    "Close the for loop with closing brace.",
                    "After loop ends, return empty string indicating no valid timestamp found.",
                    "Close the Get method with closing brace."
                  ],
                  "codeLines": [
                    "type pair struct {",
                    "    timestamp int",
                    "    value     string",
                    "}",
                    "type TimeMap struct {",
                    "    m map[string][]pair",
                    "}",
                    "func Constructor() TimeMap {",
                    "    return TimeMap{",
                    "        m: make(map[string][]pair),",
                    "    }",
                    "}",
                    "func (this *TimeMap) Set(key string, value string, timestamp int) {",
                    "    this.m[key] = append(this.m[key], pair{timestamp, value})",
                    "}",
                    "func (this *TimeMap) Get(key string, timestamp int) string {",
                    "    if _, exists := this.m[key]; !exists {",
                    "        return \"\"",
                    "    }",
                    "    pairs := this.m[key]",
                    "    l, r := 0, len(pairs)-1",
                    "    for l <= r {",
                    "        mid := (l + r) / 2",
                    "        if pairs[mid].timestamp <= timestamp {",
                    "            if mid == len(pairs)-1 || pairs[mid+1].timestamp > timestamp {",
                    "                return pairs[mid].value",
                    "            }",
                    "            l = mid + 1",
                    "        } else {",
                    "            r = mid - 1",
                    "        }",
                    "    }",
                    "    return \"\"",
                    "}"
                  ]
                }
              ],
              "timeComplexity": "O(1) for set, O(log n) for get",
              "timeDescription": "Set operation appends in constant time. Get operation uses binary search over n timestamps for a key.",
              "spaceComplexity": "O(m * n)",
              "spaceDescription": "Space for m keys, each with n timestamp-value pairs stored in arrays",
              "exampleWalkthrough": "Set('foo', 'bar', 1) stores timestamp 1. Get('foo', 1) returns 'bar'. Set('foo', 'bar2', 4) appends timestamp 4. Get('foo', 3) binary searches and finds largest timestamp <= 3 which is 1, returns 'bar'.",
              "displayOrder": 1
            },
            {
              "name": "Binary Search (Sorted Map)",
              "type": "optimal",
              "explanation": "Similar to array approach but uses language built-in binary search functions. In Go uses sort.Search to find upper bound, then returns previous element. Clean implementation leveraging standard library.",
              "languageImplementations": [
                {
                  "language": "python",
                  "semanticLines": [
                    "Define class TimeMap.",
                    "Define constructor init method with self parameter.",
                    "Initialize self dot m as empty dictionary to store key to list of timestamp-value pairs.",
                    "Define method set that accepts self, key, value, and timestamp.",
                    "Check if key is not in self dot m.",
                    "If not exists, initialize self dot m at key as empty list.",
                    "Append tuple of timestamp and value to self dot m at key.",
                    "Define method get that accepts self, key, and timestamp.",
                    "Check if key is not in self dot m.",
                    "If not exists, return empty string.",
                    "Import bisect_right from bisect module.",
                    "Store self dot m at key in variable pairs.",
                    "Call bisect_right on pairs with tuple of timestamp plus one and empty string to find upper bound, store in idx.",
                    "Check if idx equals zero indicating no valid timestamp.",
                    "If zero, return empty string.",
                    "Return value at index idx minus one in pairs as closest match."
                  ],
                  "codeLines": [
                    "class TimeMap:",
                    "    def __init__(self):",
                    "        self.m = {}",
                    "    def set(self, key, value, timestamp):",
                    "        if key not in self.m:",
                    "            self.m[key] = []",
                    "        self.m[key].append((timestamp, value))",
                    "    def get(self, key, timestamp):",
                    "        if key not in self.m:",
                    "            return \"\"",
                    "        from bisect import bisect_right",
                    "        pairs = self.m[key]",
                    "        idx = bisect_right(pairs, (timestamp + 1, \"\"))",
                    "        if idx == 0:",
                    "            return \"\"",
                    "        return pairs[idx - 1][1]"
                  ]
                },
                {
                  "language": "javascript",
                  "semanticLines": [
                    "Define class TimeMap with constructor function.",
                    "Initialize this dot m as empty object to store key to array of timestamp-value pairs.",
                    "Close the constructor with closing brace.",
                    "Define method set that accepts key, value, and timestamp.",
                    "Check if this dot m at key is undefined.",
                    "If undefined, initialize this dot m at key as empty array.",
                    "Close the if block with closing brace.",
                    "Push object with timestamp and value properties to this dot m at key.",
                    "Close the set method with closing brace.",
                    "Define method get that accepts key and timestamp.",
                    "Check if this dot m at key is undefined.",
                    "If undefined, return empty string.",
                    "Close the if block with closing brace.",
                    "Store this dot m at key in constant pairs.",
                    "Declare l initialized to zero and r initialized to length of pairs.",
                    "Start while loop that continues while l is less than r.",
                    "Declare constant mid by floor dividing l plus r by two.",
                    "Check if timestamp property of pairs at index mid is greater than timestamp.",
                    "If greater, set r to mid.",
                    "Close the if block with closing brace.",
                    "Otherwise timestamp at mid is less than or equal.",
                    "Set l to mid plus one.",
                    "Close the else block with closing brace.",
                    "Close the while loop with closing brace.",
                    "Check if l equals zero indicating no valid timestamp.",
                    "If zero, return empty string.",
                    "Close the if block with closing brace.",
                    "Return value property of pairs at index l minus one as closest match.",
                    "Close the get method with closing brace."
                  ],
                  "codeLines": [
                    "class TimeMap {",
                    "    constructor() {",
                    "        this.m = {};",
                    "    }",
                    "    set(key, value, timestamp) {",
                    "        if (this.m[key] === undefined) {",
                    "            this.m[key] = [];",
                    "        }",
                    "        this.m[key].push({ timestamp, value });",
                    "    }",
                    "    get(key, timestamp) {",
                    "        if (this.m[key] === undefined) {",
                    "            return \"\";",
                    "        }",
                    "        const pairs = this.m[key];",
                    "        let l = 0, r = pairs.length;",
                    "        while (l < r) {",
                    "            const mid = Math.floor((l + r) / 2);",
                    "            if (pairs[mid].timestamp > timestamp) {",
                    "                r = mid;",
                    "            } else {",
                    "                l = mid + 1;",
                    "            }",
                    "        }",
                    "        if (l === 0) {",
                    "            return \"\";",
                    "        }",
                    "        return pairs[l - 1].value;",
                    "    }",
                    "}"
                  ]
                },
                {
                  "language": "go",
                  "semanticLines": [
                    "Define struct pair with timestamp as int and value as string.",
                    "Define struct TimeMap with field m as map from string to slice of pair.",
                    "Define function Constructor that returns TimeMap.",
                    "Return TimeMap with m initialized using make for map from string to slice of pair.",
                    "Close the Constructor function with closing brace.",
                    "Define method Set with receiver this pointer to TimeMap that accepts key string, value string, and timestamp int.",
                    "Append new pair with timestamp and value to this dot m at key.",
                    "Close the Set method with closing brace.",
                    "Define method Get with receiver this pointer to TimeMap that accepts key string and timestamp int and returns string.",
                    "Check if blank identifier and exists by accessing this dot m at key, negate exists.",
                    "If not exists, return empty string.",
                    "Close the if block with closing brace.",
                    "Store this dot m at key in variable pairs.",
                    "Call sort Search with length of pairs and anonymous function that takes index i and returns boolean of pairs at i dot timestamp greater than timestamp, store in idx.",
                    "Check if idx equals zero indicating no valid timestamp.",
                    "If zero, return empty string.",
                    "Close the if block with closing brace.",
                    "Return value field of pairs at index idx minus one as closest match.",
                    "Close the Get method with closing brace."
                  ],
                  "codeLines": [
                    "type pair struct {",
                    "    timestamp int",
                    "    value string",
                    "}",
                    "type TimeMap struct {",
                    "    m map[string][]pair",
                    "}",
                    "func Constructor() TimeMap {",
                    "    return TimeMap{",
                    "        m: make(map[string][]pair),",
                    "    }",
                    "}",
                    "func (this *TimeMap) Set(key string, value string, timestamp int)  {",
                    "    this.m[key] = append(this.m[key], pair{timestamp, value})",
                    "}",
                    "func (this *TimeMap) Get(key string, timestamp int) string {",
                    "    if _, exists := this.m[key]; !exists {",
                    "        return \"\"",
                    "    }",
                    "    pairs := this.m[key]",
                    "    idx := sort.Search(len(pairs), func(i int) bool {",
                    "        return pairs[i].timestamp > timestamp",
                    "    })",
                    "    if idx == 0 {",
                    "        return \"\"",
                    "    }",
                    "    return pairs[idx-1].value",
                    "}"
                  ]
                }
              ],
              "timeComplexity": "O(1) for set, O(log n) for get",
              "timeDescription": "Set operation appends in constant time. Get uses built-in binary search over n timestamps.",
              "spaceComplexity": "O(m * n)",
              "spaceDescription": "Space for m keys, each with n timestamp-value pairs",
              "exampleWalkthrough": "Set('foo', 'bar', 1). Get('foo', 1): sort.Search finds first timestamp > 1 which is index 1 (none), returns idx-1=0 with value 'bar'. Set('foo', 'bar2', 4). Get('foo', 3): finds first > 3 at index 1, returns pairs[0].value = 'bar'.",
              "displayOrder": 2
            },
            {
              "name": "Brute Force",
              "type": "alternative",
              "explanation": "Store all timestamp-value pairs for each key in nested maps. For get operation, iterate through all timestamps to find maximum timestamp less than or equal to query. Inefficient O(n) lookup but simple implementation.",
              "languageImplementations": [
                {
                  "language": "python",
                  "semanticLines": [
                    "Define class TimeMap.",
                    "Define constructor init method with self parameter.",
                    "Initialize self dot keyStore as empty dictionary to store key to dictionary of timestamp to list of values.",
                    "Define method set that accepts self, key, value, and timestamp.",
                    "Check if key is not in self dot keyStore.",
                    "If not exists, initialize self dot keyStore at key as empty dictionary.",
                    "Check if timestamp is not in self dot keyStore at key.",
                    "If not exists, initialize self dot keyStore at key at timestamp as empty list.",
                    "Append value to self dot keyStore at key at timestamp.",
                    "Define method get that accepts self, key, and timestamp.",
                    "Check if key is not in self dot keyStore.",
                    "If not exists, return empty string.",
                    "Initialize seen to zero to track maximum valid timestamp.",
                    "Start for loop iterating through each time in self dot keyStore at key.",
                    "Check if time is less than or equal to timestamp.",
                    "If less or equal, update seen to maximum of seen and time.",
                    "Check if seen equals zero indicating no valid timestamp found.",
                    "If zero, return empty string.",
                    "Store self dot keyStore at key at seen in variable values.",
                    "Return last element of values as most recent value for that timestamp."
                  ],
                  "codeLines": [
                    "class TimeMap:",
                    "    def __init__(self):",
                    "        self.keyStore = {}",
                    "    def set(self, key, value, timestamp):",
                    "        if key not in self.keyStore:",
                    "            self.keyStore[key] = {}",
                    "        if timestamp not in self.keyStore[key]:",
                    "            self.keyStore[key][timestamp] = []",
                    "        self.keyStore[key][timestamp].append(value)",
                    "    def get(self, key, timestamp):",
                    "        if key not in self.keyStore:",
                    "            return \"\"",
                    "        seen = 0",
                    "        for time in self.keyStore[key]:",
                    "            if time <= timestamp:",
                    "                seen = max(seen, time)",
                    "        if seen == 0:",
                    "            return \"\"",
                    "        values = self.keyStore[key][seen]",
                    "        return values[-1]"
                  ]
                },
                {
                  "language": "javascript",
                  "semanticLines": [
                    "Define class TimeMap with constructor function.",
                    "Initialize this dot keyStore as empty object to store key to object of timestamp to array of values.",
                    "Close the constructor with closing brace.",
                    "Define method set that accepts key, value, and timestamp.",
                    "Check if this dot keyStore at key is undefined.",
                    "If undefined, initialize this dot keyStore at key as empty object.",
                    "Close the if block with closing brace.",
                    "Check if this dot keyStore at key at timestamp is undefined.",
                    "If undefined, initialize this dot keyStore at key at timestamp as empty array.",
                    "Close the if block with closing brace.",
                    "Push value to this dot keyStore at key at timestamp.",
                    "Close the set method with closing brace.",
                    "Define method get that accepts key and timestamp.",
                    "Check if this dot keyStore at key is undefined.",
                    "If undefined, return empty string.",
                    "Close the if block with closing brace.",
                    "Declare seen initialized to zero to track maximum valid timestamp.",
                    "Start for loop iterating through each time in Object keys of this dot keyStore at key.",
                    "Convert time to integer using parseInt.",
                    "Check if time is less than or equal to timestamp.",
                    "If less or equal, update seen to maximum of seen and time using Math max.",
                    "Close the if block with closing brace.",
                    "Close the for loop with closing brace.",
                    "Check if seen equals zero indicating no valid timestamp found.",
                    "If zero, return empty string.",
                    "Close the if block with closing brace.",
                    "Store this dot keyStore at key at seen in constant values.",
                    "Return element at index length minus one of values as most recent value for that timestamp.",
                    "Close the get method with closing brace."
                  ],
                  "codeLines": [
                    "class TimeMap {",
                    "    constructor() {",
                    "        this.keyStore = {};",
                    "    }",
                    "    set(key, value, timestamp) {",
                    "        if (this.keyStore[key] === undefined) {",
                    "            this.keyStore[key] = {};",
                    "        }",
                    "        if (this.keyStore[key][timestamp] === undefined) {",
                    "            this.keyStore[key][timestamp] = [];",
                    "        }",
                    "        this.keyStore[key][timestamp].push(value);",
                    "    }",
                    "    get(key, timestamp) {",
                    "        if (this.keyStore[key] === undefined) {",
                    "            return \"\";",
                    "        }",
                    "        let seen = 0;",
                    "        for (const time of Object.keys(this.keyStore[key])) {",
                    "            const timeInt = parseInt(time);",
                    "            if (timeInt <= timestamp) {",
                    "                seen = Math.max(seen, timeInt);",
                    "            }",
                    "        }",
                    "        if (seen === 0) {",
                    "            return \"\";",
                    "        }",
                    "        const values = this.keyStore[key][seen];",
                    "        return values[values.length - 1];",
                    "    }",
                    "}"
                  ]
                },
                {
                  "language": "go",
                  "semanticLines": [
                    "Define struct TimeMap with field keyStore as map from string to map from int to slice of string.",
                    "Define function Constructor that returns TimeMap.",
                    "Return TimeMap with keyStore initialized using make for map from string to map from int to slice of string.",
                    "Close the Constructor function with closing brace.",
                    "Define method Set with receiver this pointer to TimeMap that accepts key string, value string, and timestamp int.",
                    "Check if blank identifier and exists by accessing this dot keyStore at key, negate exists.",
                    "If not exists, initialize this dot keyStore at key using make for map from int to slice of string.",
                    "Close the if block with closing brace.",
                    "Append value to this dot keyStore at key at timestamp.",
                    "Close the Set method with closing brace.",
                    "Define method Get with receiver this pointer to TimeMap that accepts key string and timestamp int and returns string.",
                    "Check if blank identifier and exists by accessing this dot keyStore at key, negate exists.",
                    "If not exists, return empty string.",
                    "Close the if block with closing brace.",
                    "Initialize seen to zero to track maximum valid timestamp.",
                    "Start for loop with time iterating through range of this dot keyStore at key with blank identifier for value.",
                    "Check if time is less than or equal to timestamp.",
                    "If less or equal, update seen to result of max function with seen and time.",
                    "Close the if block with closing brace.",
                    "Close the for loop with closing brace.",
                    "Check if seen equals zero indicating no valid timestamp found.",
                    "If zero, return empty string.",
                    "Close the if block with closing brace.",
                    "Store this dot keyStore at key at seen in variable values.",
                    "Return element at index length of values minus one as most recent value for that timestamp.",
                    "Close the Get method with closing brace.",
                    "Define helper function max that accepts two integers a and b and returns int.",
                    "Check if a is greater than b.",
                    "If greater, return a.",
                    "Close the if block with closing brace.",
                    "Return b.",
                    "Close the max function with closing brace."
                  ],
                  "codeLines": [
                    "type TimeMap struct {",
                    "    keyStore map[string]map[int][]string",
                    "}",
                    "func Constructor() TimeMap {",
                    "    return TimeMap{",
                    "        keyStore: make(map[string]map[int][]string),",
                    "    }",
                    "}",
                    "func (this *TimeMap) Set(key string, value string, timestamp int) {",
                    "    if _, exists := this.keyStore[key]; !exists {",
                    "        this.keyStore[key] = make(map[int][]string)",
                    "    }",
                    "    this.keyStore[key][timestamp] = append(this.keyStore[key][timestamp], value)",
                    "}",
                    "func (this *TimeMap) Get(key string, timestamp int) string {",
                    "    if _, exists := this.keyStore[key]; !exists {",
                    "        return \"\"",
                    "    }",
                    "    seen := 0",
                    "    for time := range this.keyStore[key] {",
                    "        if time <= timestamp {",
                    "            seen = max(seen, time)",
                    "        }",
                    "    }",
                    "    if seen == 0 {",
                    "        return \"\"",
                    "    }",
                    "    values := this.keyStore[key][seen]",
                    "    return values[len(values)-1]",
                    "}",
                    "func max(a, b int) int {",
                    "    if a > b {",
                    "        return a",
                    "    }",
                    "    return b",
                    "}"
                  ]
                }
              ],
              "timeComplexity": "O(1) for set, O(n) for get",
              "timeDescription": "Set operation appends in constant time. Get iterates through all n timestamps for a key.",
              "spaceComplexity": "O(m * n)",
              "spaceDescription": "Space for m keys with nested maps storing n timestamps, each potentially with multiple values",
              "exampleWalkthrough": "Set('foo', 'bar', 1) stores in nested map. Get('foo', 1) iterates all timestamps: finds 1 <= 1, seen=1, returns 'bar'. Set('foo', 'bar2', 4). Get('foo', 3) iterates: 1 <= 3 (seen=1), 4 > 3 (skip), returns values[1][-1] = 'bar'.",
              "displayOrder": 3
            }
          ],
          "tags": [
            "design",
            "binary-search",
            "hash-table",
            "data-structure",
            "leetcode-medium"
          ],
          "nextReviewDate": null,
          "reviewCount": 0,
          "easeFactor": 2.5,
          "consecutiveCorrect": 0
        }
      ]
    }
  ]
}