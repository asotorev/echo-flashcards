{
  "decks": [
    {
      "name": "LeetCode #704 - Binary Search",
      "description": "Search for a target value in a sorted array using binary search",
      "cards": [
        {
          "id": "leetcode_704_binary_search",
          "question": "Binary Search: Given an array of integers nums which is sorted in ascending order, and an integer target, write a function to search target in nums. If target exists, then return its index. Otherwise, return -1. You must write an algorithm with O(log n) runtime complexity.",
          "solutions": [
            {
              "name": "Iterative Binary Search",
              "type": "optimal",
              "explanation": "Classic iterative binary search using two pointers. Compare middle element with target and adjust search range accordingly. Most efficient with O(1) space complexity.",
              "languageImplementations": [
                {
                  "language": "python",
                  "semanticLines": [
                    "Define function search that accepts array nums and target",
                    "Initialize l to zero and r to length of nums minus one",
                    "Start while loop that continues while l is less than or equal to r",
                    "Calculate midpoint m by adding l to the result of r minus l divided by two using integer division",
                    "Check if element at index m in nums is greater than target",
                    "If greater, set r to m minus one to search left half",
                    "Otherwise check if element at index m in nums is less than target",
                    "If less, set l to m plus one to search right half",
                    "Otherwise if element equals target",
                    "Return m as the target index was found",
                    "After loop ends, return negative one indicating target not found"
                  ],
                  "codeLines": [
                    "def search(nums, target):",
                    "    l, r = 0, len(nums) - 1",
                    "    while l <= r:",
                    "        m = l + (r - l) // 2",
                    "        if nums[m] > target:",
                    "            r = m - 1",
                    "        elif nums[m] < target:",
                    "            l = m + 1",
                    "        else:",
                    "            return m",
                    "    return -1"
                  ]
                },
                {
                  "language": "javascript",
                  "semanticLines": [
                    "Define function search that accepts array nums and target",
                    "Declare l initialized to zero and r initialized to length of nums minus one",
                    "Start while loop that continues while l is less than or equal to r",
                    "Declare constant m by adding l to floor of r minus l divided by two",
                    "Check if element at index m in nums is greater than target",
                    "If greater, set r to m minus one to search left half",
                    "Close the if block with closing brace",
                    "Otherwise check if element at index m in nums is less than target",
                    "If less, set l to m plus one to search right half",
                    "Close the else if block with closing brace",
                    "Otherwise if element equals target",
                    "Return m as the target index was found",
                    "Close the else block with closing brace",
                    "Close the while loop with closing brace",
                    "After loop ends, return negative one indicating target not found",
                    "Close function with closing brace"
                  ],
                  "codeLines": [
                    "function search(nums, target) {",
                    "    let l = 0, r = nums.length - 1;",
                    "    while (l <= r) {",
                    "        const m = l + Math.floor((r - l) / 2);",
                    "        if (nums[m] > target) {",
                    "            r = m - 1;",
                    "        } else if (nums[m] < target) {",
                    "            l = m + 1;",
                    "        } else {",
                    "            return m;",
                    "        }",
                    "    }",
                    "    return -1;",
                    "}"
                  ]
                },
                {
                  "language": "go",
                  "semanticLines": [
                    "Define function search that accepts slice of integers nums and integer target and returns int",
                    "Initialize l to zero and r to length of nums minus one",
                    "Start for loop that continues while l is less than or equal to r",
                    "Calculate midpoint m by adding l to the result of r minus l divided by two using integer division",
                    "Check if element at index m in nums is greater than target",
                    "If greater, set r to m minus one to search left half",
                    "Close the if block with closing brace",
                    "Otherwise check if element at index m in nums is less than target",
                    "If less, set l to m plus one to search right half",
                    "Close the else if block with closing brace",
                    "Otherwise if element equals target",
                    "Return m as the target index was found",
                    "Close the else block with closing brace",
                    "Close the for loop with closing brace",
                    "After loop ends, return negative one indicating target not found",
                    "Close function with closing brace"
                  ],
                  "codeLines": [
                    "func search(nums []int, target int) int {",
                    "    l, r := 0, len(nums)-1",
                    "    for l <= r {",
                    "        m := l + (r-l)/2",
                    "        if nums[m] > target {",
                    "            r = m - 1",
                    "        } else if nums[m] < target {",
                    "            l = m + 1",
                    "        } else {",
                    "            return m",
                    "        }",
                    "    }",
                    "    return -1",
                    "}"
                  ]
                }
              ],
              "timeComplexity": "O(log n)",
              "timeDescription": "Logarithmic time as search space is halved in each iteration",
              "spaceComplexity": "O(1)",
              "spaceDescription": "Constant space using only pointers, no additional data structures",
              "exampleWalkthrough": "Given nums=[-1,0,3,5,9,12] and target=9: Initial l=0, r=5. First iteration: m=2, nums[2]=3 < 9, so l=3. Second iteration: m=4, nums[4]=9 == 9, return 4.",
              "displayOrder": 1
            },
            {
              "name": "Recursive Binary Search",
              "type": "optimal",
              "explanation": "Classic recursive binary search approach. Divides search space in half recursively until target is found or range becomes invalid. Uses call stack for tracking search ranges.",
              "languageImplementations": [
                {
                  "language": "python",
                  "semanticLines": [
                    "Define function search that accepts array nums and target",
                    "Define nested function binarySearch that accepts left boundary l and right boundary r",
                    "Check if l is greater than r indicating invalid range",
                    "If invalid, return negative one as target not found",
                    "Calculate midpoint m by adding l to the result of r minus l divided by two using integer division",
                    "Check if element at index m in nums equals target",
                    "If equal, return m as target index found",
                    "Check if element at index m in nums is less than target",
                    "If less, recursively call binarySearch with m plus one as new left and r as right to search right half",
                    "Otherwise recursively call binarySearch with l as left and m minus one as new right to search left half",
                    "Call binarySearch with initial range from zero to length of nums minus one and return the result"
                  ],
                  "codeLines": [
                    "def search(nums, target):",
                    "    def binarySearch(l, r):",
                    "        if l > r:",
                    "            return -1",
                    "        m = l + (r - l) // 2",
                    "        if nums[m] == target:",
                    "            return m",
                    "        if nums[m] < target:",
                    "            return binarySearch(m + 1, r)",
                    "        return binarySearch(l, m - 1)",
                    "    return binarySearch(0, len(nums) - 1)"
                  ]
                },
                {
                  "language": "javascript",
                  "semanticLines": [
                    "Define function search that accepts array nums and target",
                    "Define nested function binarySearch that accepts left boundary l and right boundary r",
                    "Check if l is greater than r indicating invalid range",
                    "If invalid, return negative one as target not found",
                    "Close the if block with closing brace",
                    "Declare constant m by adding l to floor of r minus l divided by two",
                    "Check if element at index m in nums equals target",
                    "If equal, return m as target index found",
                    "Close the if block with closing brace",
                    "Check if element at index m in nums is less than target",
                    "If less, recursively call binarySearch with m plus one as new left and r as right to search right half",
                    "Close the if block with closing brace",
                    "Otherwise recursively call binarySearch with l as left and m minus one as new right to search left half",
                    "Close the binarySearch function with closing brace",
                    "Call binarySearch with initial range from zero to length of nums minus one and return the result",
                    "Close function with closing brace"
                  ],
                  "codeLines": [
                    "function search(nums, target) {",
                    "    function binarySearch(l, r) {",
                    "        if (l > r) {",
                    "            return -1;",
                    "        }",
                    "        const m = l + Math.floor((r - l) / 2);",
                    "        if (nums[m] === target) {",
                    "            return m;",
                    "        }",
                    "        if (nums[m] < target) {",
                    "            return binarySearch(m + 1, r);",
                    "        }",
                    "        return binarySearch(l, m - 1);",
                    "    }",
                    "    return binarySearch(0, nums.length - 1);",
                    "}"
                  ]
                },
                {
                  "language": "go",
                  "semanticLines": [
                    "Define helper function binarySearch that accepts left l, right r, slice nums, and target, returns int",
                    "Check if l is greater than r indicating invalid range",
                    "If invalid, return negative one as target not found",
                    "Close the if block with closing brace",
                    "Calculate midpoint m by adding l to the result of r minus l divided by two using integer division",
                    "Check if element at index m in nums equals target",
                    "If equal, return m as target index found",
                    "Close the if block with closing brace",
                    "Check if element at index m in nums is less than target",
                    "If less, recursively call binarySearch with m plus one, r, nums, and target to search right half",
                    "Close the if block with closing brace",
                    "Otherwise recursively call binarySearch with l, m minus one, nums, and target to search left half",
                    "Close the binarySearch function with closing brace",
                    "Define function search that accepts slice of integers nums and integer target and returns int",
                    "Call binarySearch with initial range from zero to length of nums minus one, nums, and target, then return result",
                    "Close function with closing brace"
                  ],
                  "codeLines": [
                    "func binarySearch(l, r int, nums []int, target int) int {",
                    "    if l > r {",
                    "        return -1",
                    "    }",
                    "    m := l + (r-l)/2",
                    "    if nums[m] == target {",
                    "        return m",
                    "    }",
                    "    if nums[m] < target {",
                    "        return binarySearch(m+1, r, nums, target)",
                    "    }",
                    "    return binarySearch(l, m-1, nums, target)",
                    "}",
                    "func search(nums []int, target int) int {",
                    "    return binarySearch(0, len(nums)-1, nums, target)",
                    "}"
                  ]
                }
              ],
              "timeComplexity": "O(log n)",
              "timeDescription": "Logarithmic time as search space is halved in each recursive call",
              "spaceComplexity": "O(log n)",
              "spaceDescription": "Space used by recursion call stack, depth equals log n",
              "exampleWalkthrough": "Given nums=[-1,0,3,5,9,12] and target=9: Call with l=0,r=5. m=2, nums[2]=3 < 9, recurse with l=3,r=5. m=4, nums[4]=9 found, return 4.",
              "displayOrder": 2
            },
            {
              "name": "Lower Bound",
              "type": "alternative",
              "explanation": "Binary search variant that finds the lower bound (leftmost position where target could be inserted). Useful for finding first occurrence of target in sorted array.",
              "languageImplementations": [
                {
                  "language": "python",
                  "semanticLines": [
                    "Define function search that accepts array nums and target",
                    "Initialize l to zero and r to length of nums",
                    "Start while loop that continues while l is less than r",
                    "Calculate midpoint m by adding l to the result of r minus l divided by two using integer division",
                    "Check if element at index m in nums is greater than or equal to target",
                    "If greater or equal, set r to m to search left half including m",
                    "Otherwise if element is less than target",
                    "Set l to m plus one to search right half",
                    "Check if l is less than length of nums and element at index l equals target",
                    "If both conditions true, return l as target index found",
                    "Otherwise return negative one indicating target not found"
                  ],
                  "codeLines": [
                    "def search(nums, target):",
                    "    l, r = 0, len(nums)",
                    "    while l < r:",
                    "        m = l + (r - l) // 2",
                    "        if nums[m] >= target:",
                    "            r = m",
                    "        else:",
                    "            l = m + 1",
                    "    if l < len(nums) and nums[l] == target:",
                    "        return l",
                    "    return -1"
                  ]
                },
                {
                  "language": "javascript",
                  "semanticLines": [
                    "Define function search that accepts array nums and target",
                    "Declare l initialized to zero and r initialized to length of nums",
                    "Start while loop that continues while l is less than r",
                    "Declare constant m by adding l to floor of r minus l divided by two",
                    "Check if element at index m in nums is greater than or equal to target",
                    "If greater or equal, set r to m to search left half including m",
                    "Close the if block with closing brace",
                    "Otherwise if element is less than target",
                    "Set l to m plus one to search right half",
                    "Close the else block with closing brace",
                    "Close the while loop with closing brace",
                    "Check if l is less than length of nums and element at index l equals target",
                    "If both conditions true, return l as target index found",
                    "Close the if block with closing brace",
                    "Otherwise return negative one indicating target not found",
                    "Close function with closing brace"
                  ],
                  "codeLines": [
                    "function search(nums, target) {",
                    "    let l = 0, r = nums.length;",
                    "    while (l < r) {",
                    "        const m = l + Math.floor((r - l) / 2);",
                    "        if (nums[m] >= target) {",
                    "            r = m;",
                    "        } else {",
                    "            l = m + 1;",
                    "        }",
                    "    }",
                    "    if (l < nums.length && nums[l] === target) {",
                    "        return l;",
                    "    }",
                    "    return -1;",
                    "}"
                  ]
                },
                {
                  "language": "go",
                  "semanticLines": [
                    "Define function search that accepts slice of integers nums and integer target and returns int",
                    "Initialize l to zero and r to length of nums",
                    "Start for loop that continues while l is less than r",
                    "Calculate midpoint m by adding l to the result of r minus l divided by two using integer division",
                    "Check if element at index m in nums is greater than or equal to target",
                    "If greater or equal, set r to m to search left half including m",
                    "Close the if block with closing brace",
                    "Otherwise if element is less than target",
                    "Set l to m plus one to search right half",
                    "Close the else block with closing brace",
                    "Close the for loop with closing brace",
                    "Check if l is less than length of nums and element at index l equals target",
                    "If both conditions true, return l as target index found",
                    "Close the if block with closing brace",
                    "Otherwise return negative one indicating target not found",
                    "Close function with closing brace"
                  ],
                  "codeLines": [
                    "func search(nums []int, target int) int {",
                    "    l, r := 0, len(nums)",
                    "    for l < r {",
                    "        m := l + (r-l)/2",
                    "        if nums[m] >= target {",
                    "            r = m",
                    "        } else {",
                    "            l = m + 1",
                    "        }",
                    "    }",
                    "    if l < len(nums) && nums[l] == target {",
                    "        return l",
                    "    }",
                    "    return -1",
                    "}"
                  ]
                }
              ],
              "timeComplexity": "O(log n)",
              "timeDescription": "Logarithmic time as search space is halved in each iteration",
              "spaceComplexity": "O(1)",
              "spaceDescription": "Constant space using only pointers, no additional data structures",
              "exampleWalkthrough": "Given nums=[-1,0,3,5,9,12] and target=9: Initial l=0, r=6. First: m=3, nums[3]=5 < 9, l=4. Second: m=5, nums[5]=12 >= 9, r=5. Third: m=4, nums[4]=9 >= 9, r=4. Loop ends with l=4, nums[4]=9, return 4.",
              "displayOrder": 3
            },
            {
              "name": "Upper Bound",
              "type": "alternative",
              "explanation": "Binary search variant that finds the upper bound (rightmost position where target could be inserted). Searches for position after last occurrence of target.",
              "languageImplementations": [
                {
                  "language": "python",
                  "semanticLines": [
                    "Define function search that accepts array nums and target",
                    "Initialize l to zero and r to length of nums",
                    "Start while loop that continues while l is less than r",
                    "Calculate midpoint m by adding l to the result of r minus l divided by two using integer division",
                    "Check if element at index m in nums is greater than target",
                    "If greater, set r to m to search left half including m",
                    "Otherwise if element is less than or equal to target",
                    "Set l to m plus one to search right half",
                    "Check if l is greater than zero and element at index l minus one equals target",
                    "If both conditions true, return l minus one as target index found",
                    "Otherwise return negative one indicating target not found"
                  ],
                  "codeLines": [
                    "def search(nums, target):",
                    "    l, r = 0, len(nums)",
                    "    while l < r:",
                    "        m = l + (r - l) // 2",
                    "        if nums[m] > target:",
                    "            r = m",
                    "        else:",
                    "            l = m + 1",
                    "    if l > 0 and nums[l - 1] == target:",
                    "        return l - 1",
                    "    return -1"
                  ]
                },
                {
                  "language": "javascript",
                  "semanticLines": [
                    "Define function search that accepts array nums and target",
                    "Declare l initialized to zero and r initialized to length of nums",
                    "Start while loop that continues while l is less than r",
                    "Declare constant m by adding l to floor of r minus l divided by two",
                    "Check if element at index m in nums is greater than target",
                    "If greater, set r to m to search left half including m",
                    "Close the if block with closing brace",
                    "Otherwise if element is less than or equal to target",
                    "Set l to m plus one to search right half",
                    "Close the else block with closing brace",
                    "Close the while loop with closing brace",
                    "Check if l is greater than zero and element at index l minus one equals target",
                    "If both conditions true, return l minus one as target index found",
                    "Close the if block with closing brace",
                    "Otherwise return negative one indicating target not found",
                    "Close function with closing brace"
                  ],
                  "codeLines": [
                    "function search(nums, target) {",
                    "    let l = 0, r = nums.length;",
                    "    while (l < r) {",
                    "        const m = l + Math.floor((r - l) / 2);",
                    "        if (nums[m] > target) {",
                    "            r = m;",
                    "        } else {",
                    "            l = m + 1;",
                    "        }",
                    "    }",
                    "    if (l > 0 && nums[l - 1] === target) {",
                    "        return l - 1;",
                    "    }",
                    "    return -1;",
                    "}"
                  ]
                },
                {
                  "language": "go",
                  "semanticLines": [
                    "Define function search that accepts slice of integers nums and integer target and returns int",
                    "Initialize l to zero and r to length of nums",
                    "Start for loop that continues while l is less than r",
                    "Calculate midpoint m by adding l to the result of r minus l divided by two using integer division",
                    "Check if element at index m in nums is greater than target",
                    "If greater, set r to m to search left half including m",
                    "Close the if block with closing brace",
                    "Otherwise if element is less than or equal to target",
                    "Set l to m plus one to search right half",
                    "Close the else block with closing brace",
                    "Close the for loop with closing brace",
                    "Check if l is greater than zero and element at index l minus one equals target",
                    "If both conditions true, return l minus one as target index found",
                    "Close the if block with closing brace",
                    "Otherwise return negative one indicating target not found",
                    "Close function with closing brace"
                  ],
                  "codeLines": [
                    "func search(nums []int, target int) int {",
                    "    l, r := 0, len(nums)",
                    "    for l < r {",
                    "        m := l + (r-l)/2",
                    "        if nums[m] > target {",
                    "            r = m",
                    "        } else {",
                    "            l = m + 1",
                    "        }",
                    "    }",
                    "    if l > 0 && nums[l-1] == target {",
                    "        return l - 1",
                    "    }",
                    "    return -1",
                    "}"
                  ]
                }
              ],
              "timeComplexity": "O(log n)",
              "timeDescription": "Logarithmic time as search space is halved in each iteration",
              "spaceComplexity": "O(1)",
              "spaceDescription": "Constant space using only pointers, no additional data structures",
              "exampleWalkthrough": "Given nums=[-1,0,3,5,9,12] and target=9: Initial l=0, r=6. First: m=3, nums[3]=5 <= 9, l=4. Second: m=5, nums[5]=12 > 9, r=5. Third: m=4, nums[4]=9 <= 9, l=5. Loop ends, l=5, nums[4]=9, return 4.",
              "displayOrder": 4
            },
            {
              "name": "Built-In Function",
              "type": "alternative",
              "explanation": "Use language built-in binary search functions. Python uses bisect_left for lower bound search, Go uses sort.Search. Demonstrates practical usage of standard library.",
              "languageImplementations": [
                {
                  "language": "python",
                  "semanticLines": [
                    "Define function search that accepts array nums and target",
                    "Import bisect_left from bisect module",
                    "Call bisect_left with nums and target to find lower bound index and store in index",
                    "Check if index is less than length of nums and element at index equals target",
                    "If both conditions true, return index as target found",
                    "Otherwise return negative one indicating target not found"
                  ],
                  "codeLines": [
                    "def search(nums, target):",
                    "    from bisect import bisect_left",
                    "    index = bisect_left(nums, target)",
                    "    if index < len(nums) and nums[index] == target:",
                    "        return index",
                    "    return -1"
                  ]
                },
                {
                  "language": "javascript",
                  "semanticLines": [
                    "Define function search that accepts array nums and target",
                    "Declare l initialized to zero and r initialized to length of nums",
                    "Start while loop that continues while l is less than r",
                    "Declare constant m by adding l to floor of r minus l divided by two",
                    "Check if element at index m in nums is less than target",
                    "If less, set l to m plus one to search right half",
                    "Close the if block with closing brace",
                    "Otherwise if element is greater than or equal to target",
                    "Set r to m to search left half including m",
                    "Close the else block with closing brace",
                    "Close the while loop with closing brace",
                    "Check if l is less than length of nums and element at index l equals target",
                    "If both conditions true, return l as target index found",
                    "Close the if block with closing brace",
                    "Otherwise return negative one indicating target not found",
                    "Close function with closing brace"
                  ],
                  "codeLines": [
                    "function search(nums, target) {",
                    "    let l = 0, r = nums.length;",
                    "    while (l < r) {",
                    "        const m = l + Math.floor((r - l) / 2);",
                    "        if (nums[m] < target) {",
                    "            l = m + 1;",
                    "        } else {",
                    "            r = m;",
                    "        }",
                    "    }",
                    "    if (l < nums.length && nums[l] === target) {",
                    "        return l;",
                    "    }",
                    "    return -1;",
                    "}"
                  ]
                },
                {
                  "language": "go",
                  "semanticLines": [
                    "Define function search that accepts slice of integers nums and integer target and returns int",
                    "Call sort Search with length of nums and anonymous function that takes index i and returns boolean of nums at i greater than or equal to target, store result in index",
                    "Check if index is less than length of nums and element at index equals target",
                    "If both conditions true, return index as target found",
                    "Close the if block with closing brace",
                    "Otherwise return negative one indicating target not found",
                    "Close function with closing brace"
                  ],
                  "codeLines": [
                    "func search(nums []int, target int) int {",
                    "    index := sort.Search(len(nums), func(i int) bool { return nums[i] >= target })",
                    "    if index < len(nums) && nums[index] == target {",
                    "        return index",
                    "    }",
                    "    return -1",
                    "}"
                  ]
                }
              ],
              "timeComplexity": "O(log n)",
              "timeDescription": "Logarithmic time as built-in functions use binary search internally",
              "spaceComplexity": "O(1)",
              "spaceDescription": "Constant space, built-in functions use iterative approach",
              "exampleWalkthrough": "Given nums=[-1,0,3,5,9,12] and target=9: bisect_left/sort.Search finds index 4 where target would be inserted. Verify nums[4]=9 matches target, return 4.",
              "displayOrder": 5
            }
          ],
          "tags": [
            "binary-search",
            "array",
            "divide-and-conquer",
            "leetcode-easy"
          ],
          "nextReviewDate": null,
          "reviewCount": 0,
          "easeFactor": 2.5,
          "consecutiveCorrect": 0
        }
      ]
    }
  ]
}
