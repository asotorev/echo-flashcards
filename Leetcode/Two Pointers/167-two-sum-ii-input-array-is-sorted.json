{
  "decks": [
    {
      "name": "LeetCode #167 - Two Sum II - Input Array Is Sorted",
      "description": "Find two numbers in a sorted array that add up to a target value",
      "cards": [
        {
          "id": "leetcode_167_two_sum_ii",
          "question": "Two Sum II - Input Array Is Sorted: Given a 1-indexed array of integers numbers that is already sorted in non-decreasing order, find two numbers such that they add up to a specific target number. Let these two numbers be numbers[index1] and numbers[index2] where 1 <= index1 < index2 <= numbers.length. Return the indices of the two numbers, index1 and index2, added by one as an integer array [index1, index2] of length 2. You may not use the same element twice.",
          "solutions": [
            {
              "name": "Two Pointers",
              "type": "optimal",
              "explanation": "Use two pointers starting from the beginning and end of the array. Since the array is sorted, if the sum is too large, move the right pointer left; if too small, move the left pointer right. This takes advantage of the sorted property for O(n) time with O(1) space.",
              "languageImplementations": [
                {
                  "language": "python",
                  "semanticLines": [
                    "Define function twoSum that accepts array numbers and integer target.",
                    "Initialize variable l to zero for left pointer and variable r to length of numbers minus one for right pointer.",
                    "Create empty line.",
                    "Loop while l is less than r.",
                    "Get numbers at l, get numbers at r, add them together, and store in curSum.",
                    "Check if curSum is greater than target.",
                    "If greater, decrement r by one.",
                    "Otherwise check if curSum is less than target.",
                    "If less, increment l by one.",
                    "Otherwise curSum equals target.",
                    "Return array containing l plus one and r plus one.",
                    "After loop, return empty array."
                  ],
                  "codeLines": [
                    "def twoSum(numbers, target):",
                    "    l, r = 0, len(numbers) - 1",
                    "    ",
                    "    while l < r:",
                    "        curSum = numbers[l] + numbers[r]",
                    "        if curSum > target:",
                    "            r -= 1",
                    "        elif curSum < target:",
                    "            l += 1",
                    "        else:",
                    "            return [l + 1, r + 1]",
                    "    return []"
                  ]
                },
                {
                  "language": "javascript",
                  "semanticLines": [
                    "Define function twoSum that accepts array numbers and integer target.",
                    "Initialize variable l to zero for left pointer and variable r to length of numbers minus one for right pointer.",
                    "Create empty line.",
                    "Loop while l is less than r.",
                    "Get numbers at l, get numbers at r, add them together, and store in constant curSum.",
                    "Check if curSum is greater than target.",
                    "If greater, decrement r by one.",
                    "Close the if statement with closing brace.",
                    "Otherwise check if curSum is less than target.",
                    "If less, increment l by one.",
                    "Close the else if statement with closing brace.",
                    "Otherwise curSum equals target.",
                    "Return array containing l plus one and r plus one.",
                    "Close the else block with closing brace.",
                    "Close the while loop with closing brace.",
                    "After loop, return empty array.",
                    "Close the function with closing brace."
                  ],
                  "codeLines": [
                    "function twoSum(numbers, target) {",
                    "    let l = 0, r = numbers.length - 1;",
                    "    ",
                    "    while (l < r) {",
                    "        const curSum = numbers[l] + numbers[r];",
                    "        if (curSum > target) {",
                    "            r--;",
                    "        } else if (curSum < target) {",
                    "            l++;",
                    "        } else {",
                    "            return [l + 1, r + 1];",
                    "        }",
                    "    }",
                    "    return [];",
                    "}"
                  ]
                },
                {
                  "language": "go",
                  "semanticLines": [
                    "Define function twoSum that accepts slice of integers numbers and integer target and returns slice of integers.",
                    "Initialize variable l to zero for left pointer and variable r to length of numbers minus one for right pointer.",
                    "Create empty line.",
                    "Loop while l is less than r using for loop.",
                    "Get numbers at l, get numbers at r, add them together, and store in curSum.",
                    "Check if curSum is greater than target.",
                    "If greater, decrement r by one.",
                    "Close the if block with closing brace.",
                    "Otherwise check if curSum is less than target.",
                    "If less, increment l by one.",
                    "Close the else if block with closing brace.",
                    "Otherwise curSum equals target.",
                    "Return integer slice containing l plus one and r plus one.",
                    "Close the else block with closing brace.",
                    "Close the for loop with closing brace.",
                    "After loop, return empty integer slice.",
                    "Close the function with closing brace."
                  ],
                  "codeLines": [
                    "func twoSum(numbers []int, target int) []int {",
                    "    l, r := 0, len(numbers) - 1",
                    "",
                    "    for l < r {",
                    "        curSum := numbers[l] + numbers[r]",
                    "        if curSum > target {",
                    "            r--",
                    "        } else if curSum < target {",
                    "            l++",
                    "        } else {",
                    "            return []int{l + 1, r + 1}",
                    "        }",
                    "    }",
                    "    return []int{}",
                    "}"
                  ]
                }
              ],
              "timeComplexity": "O(n)",
              "timeDescription": "Linear time where n is the length of the array. We traverse the array at most once with the two pointers.",
              "spaceComplexity": "O(1)",
              "spaceDescription": "Constant extra space. We only use two pointer variables.",
              "exampleWalkthrough": "For numbers=[2,7,11,15], target=9: Start with l=0(2), r=3(15). Sum=17>9, move r to 2(11). Sum=13>9, move r to 1(7). Sum=9, return [1,2] (1-indexed).",
              "displayOrder": 1
            },
            {
              "name": "Hash Map",
              "type": "alternative",
              "explanation": "Use a hash map to store each number and its 1-indexed position as we iterate. For each number, check if its complement (target - number) exists in the map. Fast but uses extra space.",
              "languageImplementations": [
                {
                  "language": "python",
                  "semanticLines": [
                    "Define function twoSum that accepts array numbers and integer target.",
                    "Create empty dictionary mp to store number to index mapping.",
                    "Loop through indices from zero to length of numbers.",
                    "Calculate target minus numbers at i and store in tmp.",
                    "Check if tmp exists in mp.",
                    "If exists, return array containing mp at tmp and i plus one.",
                    "Store i plus one in mp at key numbers at i.",
                    "After loop, return empty array."
                  ],
                  "codeLines": [
                    "def twoSum(numbers, target):",
                    "    mp = {}",
                    "    for i in range(len(numbers)):",
                    "        tmp = target - numbers[i]",
                    "        if tmp in mp:",
                    "            return [mp[tmp], i + 1]",
                    "        mp[numbers[i]] = i + 1",
                    "    return []"
                  ]
                },
                {
                  "language": "javascript",
                  "semanticLines": [
                    "Define function twoSum that accepts array numbers and integer target.",
                    "Create new Map object mp to store number to index mapping.",
                    "Loop with i from zero while i less than length of numbers incrementing i.",
                    "Calculate target minus numbers at i and store in constant tmp.",
                    "Check if mp has tmp using the has method.",
                    "If exists, return array containing mp get tmp and i plus one.",
                    "Close the if statement with closing brace.",
                    "Store i plus one in mp at key numbers at i using set method.",
                    "Close the for loop with closing brace.",
                    "After loop, return empty array.",
                    "Close the function with closing brace."
                  ],
                  "codeLines": [
                    "function twoSum(numbers, target) {",
                    "    const mp = new Map();",
                    "    for (let i = 0; i < numbers.length; i++) {",
                    "        const tmp = target - numbers[i];",
                    "        if (mp.has(tmp)) {",
                    "            return [mp.get(tmp), i + 1];",
                    "        }",
                    "        mp.set(numbers[i], i + 1);",
                    "    }",
                    "    return [];",
                    "}"
                  ]
                },
                {
                  "language": "go",
                  "semanticLines": [
                    "Define function twoSum that accepts slice of integers numbers and integer target and returns slice of integers.",
                    "Create map mp with integer keys and integer values using make.",
                    "Loop with i from zero while i less than length of numbers incrementing i.",
                    "Calculate target minus numbers at i and store in tmp.",
                    "Check if tmp exists in mp by retrieving value val and existence flag exists.",
                    "If exists, return integer slice containing val and i plus one.",
                    "Close the if block with closing brace.",
                    "Store i plus one in mp at key numbers at i.",
                    "Close the for loop with closing brace.",
                    "After loop, return empty integer slice.",
                    "Close the function with closing brace."
                  ],
                  "codeLines": [
                    "func twoSum(numbers []int, target int) []int {",
                    "    mp := make(map[int]int)",
                    "    for i := 0; i < len(numbers); i++ {",
                    "        tmp := target - numbers[i]",
                    "        if val, exists := mp[tmp]; exists {",
                    "            return []int{val, i + 1}",
                    "        }",
                    "        mp[numbers[i]] = i + 1",
                    "    }",
                    "    return []int{}",
                    "}"
                  ]
                }
              ],
              "timeComplexity": "O(n)",
              "timeDescription": "Linear time where n is the length of the array. We traverse the array once, with O(1) hash map lookups.",
              "spaceComplexity": "O(n)",
              "spaceDescription": "Linear space for the hash map. In the worst case, we store all n numbers before finding the pair.",
              "exampleWalkthrough": "For numbers=[2,7,11,15], target=9: i=0, tmp=7, mp empty, add mp[2]=1. i=1, tmp=2, found in mp at index 1, return [1,2].",
              "displayOrder": 2
            },
            {
              "name": "Binary Search",
              "type": "alternative",
              "explanation": "For each element, use binary search to find its complement in the remaining sorted array. Takes advantage of the sorted property but slower than two pointers due to the logarithmic factor.",
              "languageImplementations": [
                {
                  "language": "python",
                  "semanticLines": [
                    "Define function twoSum that accepts array numbers and integer target.",
                    "Loop through indices from zero to length of numbers.",
                    "Initialize l to i plus one and r to length of numbers minus one for binary search bounds.",
                    "Calculate target minus numbers at i and store in tmp.",
                    "Loop while l is less than or equal to r.",
                    "Calculate l plus the floor of r minus l divided by two and store in mid.",
                    "Check if numbers at mid equals tmp.",
                    "If equal, return array containing i plus one and mid plus one.",
                    "Otherwise check if numbers at mid is less than tmp.",
                    "If less, set l to mid plus one.",
                    "Otherwise numbers at mid is greater than tmp.",
                    "Set r to mid minus one.",
                    "After inner loop, return empty array."
                  ],
                  "codeLines": [
                    "def twoSum(numbers, target):",
                    "    for i in range(len(numbers)):",
                    "        l, r = i + 1, len(numbers) - 1",
                    "        tmp = target - numbers[i]",
                    "        while l <= r:",
                    "            mid = l + (r - l) // 2",
                    "            if numbers[mid] == tmp:",
                    "                return [i + 1, mid + 1]",
                    "            elif numbers[mid] < tmp:",
                    "                l = mid + 1",
                    "            else:",
                    "                r = mid - 1",
                    "    return []"
                  ]
                },
                {
                  "language": "javascript",
                  "semanticLines": [
                    "Define function twoSum that accepts array numbers and integer target.",
                    "Loop with i from zero while i less than length of numbers incrementing i.",
                    "Initialize variable l to i plus one and variable r to length of numbers minus one for binary search bounds.",
                    "Calculate target minus numbers at i and store in variable tmp.",
                    "Loop while l is less than or equal to r.",
                    "Calculate l plus the floor of r minus l divided by two and store in variable mid.",
                    "Check if numbers at mid equals tmp.",
                    "If equal, return array containing i plus one and mid plus one.",
                    "Close the if statement with closing brace.",
                    "Otherwise check if numbers at mid is less than tmp.",
                    "If less, set l to mid plus one.",
                    "Close the else if statement with closing brace.",
                    "Otherwise numbers at mid is greater than tmp.",
                    "Set r to mid minus one.",
                    "Close the else block with closing brace.",
                    "Close the inner while loop with closing brace.",
                    "Close the outer for loop with closing brace.",
                    "After loop, return empty array.",
                    "Close the function with closing brace."
                  ],
                  "codeLines": [
                    "function twoSum(numbers, target) {",
                    "    for (let i = 0; i < numbers.length; i++) {",
                    "        let l = i + 1, r = numbers.length - 1;",
                    "        let tmp = target - numbers[i];",
                    "        while (l <= r) {",
                    "            let mid = l + Math.floor((r - l) / 2);",
                    "            if (numbers[mid] === tmp) {",
                    "                return [i + 1, mid + 1];",
                    "            } else if (numbers[mid] < tmp) {",
                    "                l = mid + 1;",
                    "            } else {",
                    "                r = mid - 1;",
                    "            }",
                    "        }",
                    "    }",
                    "    return [];",
                    "}"
                  ]
                },
                {
                  "language": "go",
                  "semanticLines": [
                    "Define function twoSum that accepts slice of integers numbers and integer target and returns slice of integers.",
                    "Loop with i from zero while i less than length of numbers incrementing i.",
                    "Initialize variable l to i plus one and variable r to length of numbers minus one for binary search bounds.",
                    "Calculate target minus numbers at i and store in tmp.",
                    "Loop while l is less than or equal to r using for loop.",
                    "Calculate l plus integer division of r minus l by two and store in mid.",
                    "Check if numbers at mid equals tmp.",
                    "If equal, return integer slice containing i plus one and mid plus one.",
                    "Close the if block with closing brace.",
                    "Otherwise check if numbers at mid is less than tmp.",
                    "If less, set l to mid plus one.",
                    "Close the else if block with closing brace.",
                    "Otherwise numbers at mid is greater than tmp.",
                    "Set r to mid minus one.",
                    "Close the else block with closing brace.",
                    "Close the inner for loop with closing brace.",
                    "Close the outer for loop with closing brace.",
                    "After loop, return empty integer slice.",
                    "Close the function with closing brace."
                  ],
                  "codeLines": [
                    "func twoSum(numbers []int, target int) []int {",
                    "    for i := 0; i < len(numbers); i++ {",
                    "        l, r := i+1, len(numbers)-1",
                    "        tmp := target - numbers[i]",
                    "        for l <= r {",
                    "            mid := l + (r-l)/2",
                    "            if numbers[mid] == tmp {",
                    "                return []int{i + 1, mid + 1}",
                    "            } else if numbers[mid] < tmp {",
                    "                l = mid + 1",
                    "            } else {",
                    "                r = mid - 1",
                    "            }",
                    "        }",
                    "    }",
                    "    return []int{}",
                    "}"
                  ]
                }
              ],
              "timeComplexity": "O(n log n)",
              "timeDescription": "Linearithmic time where n is the length of the array. For each of n elements, we perform a binary search taking O(log n) time.",
              "spaceComplexity": "O(1)",
              "spaceDescription": "Constant extra space. We only use a few pointer variables.",
              "exampleWalkthrough": "For numbers=[2,7,11,15], target=9: i=0, search for 7 in [7,11,15]. Binary search: mid=1(7), found! Return [1,2].",
              "displayOrder": 3
            },
            {
              "name": "Brute Force",
              "type": "alternative",
              "explanation": "Try all possible pairs of numbers using nested loops. Check every combination until we find two numbers that sum to the target. Simple but inefficient with O(n²) time complexity.",
              "languageImplementations": [
                {
                  "language": "python",
                  "semanticLines": [
                    "Define function twoSum that accepts array numbers and integer target.",
                    "Loop through indices from zero to length of numbers.",
                    "Loop through indices from i plus one to length of numbers.",
                    "Get numbers at i, get numbers at j, add them together, compare if equal to target.",
                    "If equal, return array containing i plus one and j plus one.",
                    "After all iterations, return empty array."
                  ],
                  "codeLines": [
                    "def twoSum(numbers, target):",
                    "    for i in range(len(numbers)):",
                    "        for j in range(i + 1, len(numbers)):",
                    "            if numbers[i] + numbers[j] == target:",
                    "                return [i + 1, j + 1]",
                    "    return []"
                  ]
                },
                {
                  "language": "javascript",
                  "semanticLines": [
                    "Define function twoSum that accepts array numbers and integer target.",
                    "Loop with i from zero while i less than length of numbers incrementing i.",
                    "Loop with j from i plus one while j less than length of numbers incrementing j.",
                    "Get numbers at i, get numbers at j, add them together, compare if equal to target.",
                    "If equal, return array containing i plus one and j plus one.",
                    "Close the if statement with closing brace.",
                    "Close the inner for loop with closing brace.",
                    "Close the outer for loop with closing brace.",
                    "After all iterations, return empty array.",
                    "Close the function with closing brace."
                  ],
                  "codeLines": [
                    "function twoSum(numbers, target) {",
                    "    for (let i = 0; i < numbers.length; i++) {",
                    "        for (let j = i + 1; j < numbers.length; j++) {",
                    "            if (numbers[i] + numbers[j] === target) {",
                    "                return [i + 1, j + 1];",
                    "            }",
                    "        }",
                    "    }",
                    "    return [];",
                    "}"
                  ]
                },
                {
                  "language": "go",
                  "semanticLines": [
                    "Define function twoSum that accepts slice of integers numbers and integer target and returns slice of integers.",
                    "Loop with i from zero while i less than length of numbers incrementing i.",
                    "Loop with j from i plus one while j less than length of numbers incrementing j.",
                    "Get numbers at i, get numbers at j, add them together, compare if equal to target.",
                    "If equal, return integer slice containing i plus one and j plus one.",
                    "Close the if block with closing brace.",
                    "Close the inner for loop with closing brace.",
                    "Close the outer for loop with closing brace.",
                    "After all iterations, return empty integer slice.",
                    "Close the function with closing brace."
                  ],
                  "codeLines": [
                    "func twoSum(numbers []int, target int) []int {",
                    "    for i := 0; i < len(numbers); i++ {",
                    "        for j := i + 1; j < len(numbers); j++ {",
                    "            if numbers[i]+numbers[j] == target {",
                    "                return []int{i + 1, j + 1}",
                    "            }",
                    "        }",
                    "    }",
                    "    return []int{}",
                    "}"
                  ]
                }
              ],
              "timeComplexity": "O(n²)",
              "timeDescription": "Quadratic time where n is the length of the array. We check all possible pairs using nested loops.",
              "spaceComplexity": "O(1)",
              "spaceDescription": "Constant extra space. We only use loop counter variables.",
              "exampleWalkthrough": "For numbers=[2,7,11,15], target=9: Try i=0,j=1: 2+7=9, found! Return [1,2].",
              "displayOrder": 4
            }
          ],
          "tags": [
            "arrays",
            "two-pointers",
            "binary-search",
            "hash-table",
            "leetcode-medium"
          ],
          "nextReviewDate": null,
          "reviewCount": 0,
          "easeFactor": 2.5,
          "consecutiveCorrect": 0
        }
      ]
    }
  ]
}